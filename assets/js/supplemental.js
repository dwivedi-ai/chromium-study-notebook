// Chapters added after comparing the course with every supplied essay section.
// The source function is injected to keep one consistent source-link format.
export function supplemental(source) {
  return {
    rolls: {
      id:'rolls', part:'Source and build', title:'How Chromium updates its dependencies',
      deck:'Follow a V8 roll through DEPS, Git submodules, review, and CI.', deps:['checkout'], skill:'Dependency changes',
      lead:'A dependency pin is not a passive line in DEPS. It is changed by tools, often by a bot, and the resulting revision must be tested and submitted. The current Chromium tree also records many Git dependencies as submodules, creating two representations of the same commit.',
      flow:['Choose revision','Edit DEPS token','Update Git gitlink','Upload roll','Run CQ'],
      sections:[
        ['What a roll changes',`<p>A roll changes the version of a component consumed by Chromium. For a Git dependency such as V8, <code>src/DEPS</code> may construct the URL and revision from variables. A roll of V8 can therefore change <code>v8_revision</code> without touching the URL expression. In the studied tree, Chromium also records the dependency commit as a Git submodule gitlink. A correct roll keeps the DEPS value and gitlink together.</p><p>Think of a roll as an integration change. The child project may build and pass its own tests, yet its new revision may fail when built with Chromium’s API assumptions, compiler settings, or tests. The commit queue tests that integration. A machine-authored roll is still a real change to the product’s source graph.</p>`],
        ['How setdep edits without rewriting the file',`<p><code>gclient setdep</code> uses <code>gclient_eval.py</code> to parse DEPS as both a restricted syntax tree and positioned tokens. The syntax tree identifies the value to change. The token stream preserves spelling, whitespace, and comments. For V8, the tool finds the variable behind the revision and replaces its string token. <code>RenderDEPSFile</code> then writes the token stream back. It does not pretty-print a new DEPS file from an AST.</p><p>That design matters because a bot should make a small reviewable diff. If changing one revision reformatted thousands of lines, reviewers and merge tools would have trouble seeing the actual change. Try <code>gclient getdep -r src/v8</code> on a full checkout, then run <code>gclient setdep</code> on a disposable copy and inspect the diff. The exact syntax and behavior live in <code>depot_tools/gclient.py</code> and <code>gclient_eval.py</code>.</p><div class="code-path"><b>Two representations</b><code>DEPS variable → evaluated revision<br>Git index entry, mode 160000 → submodule commit</code></div>`],
        ['Why .gitmodules does not replace DEPS yet',`<p>The supplied research describes Chromium’s submodule migration as a staged change. At the pinned revision, <code>src/DEPS</code> declares <code>git_dependencies = 'SYNC'</code>. In that mode, Git submodule metadata and gitlinks coexist with DEPS; <code>gclient</code> still manages checkout policy. <code>src/.gitmodules</code> can include <code>gclient-condition</code>, which native Git does not evaluate. CIPD packages also remain in DEPS.</p><p>This is a good example of migration cost. Adding a new representation is easy compared with changing every reader of the old one. Rollers, presubmit checks, build tools, and scripts still read DEPS variables. The source archive shows both representations at this revision. It does not establish that the announced later migration stages have since happened.</p>`],
        ['What an autoroller does',`<p>Most dependency updates are automated. An autoroller chooses a newer child revision, prepares a small parent change, uploads it to Gerrit, and lets configured tryjobs test the integration. The supplied research found that Skia’s AutoRoll implementation edits DEPS directly rather than invoking <code>gclient setdep</code>; when the parent uses synchronized submodules, it also updates the gitlink. The two tools implement the same requirement from different codebases.</p><p>When a roll fails, identify which layer failed. Did the child revision break its own build? Did Chromium compile against a changed API? Did a test expose a behavior difference? Was the failure pre-existing or flaky? A rollback and a paused roller can contain a bad integration, but the engineering task is to isolate the incompatible revision or assumption. Counts of bots and rolls in the original research were measurements from September 2026, not stable properties of Chromium.</p>`],
        ['A roll as a reproducibility test',`<p>Suppose your local V8 source is newer than the V8 revision in <code>src/DEPS</code>. A local build might succeed while the commit queue fails, or the reverse. The meaningful identity is the whole checkout: Chromium commit, dependency pins, Git gitlinks, packages, generated arguments, and toolchain. Before debugging C++ source, compare those inputs.</p><p>For practice, read <code>src/DEPS</code> at the V8 variable and the V8 dependency entry, then read the <code>src/v8</code> gitlink in the index of a complete checkout. Predict what <code>gclient sync</code> would fetch. If the values differ, the first investigation is whether the checkout is unsynced or the two source representations disagree.</p>`]
      ],
      quiz:['A roll changes only v8_revision in DEPS. Is that enough to describe the submitted change?', 'At the studied SYNC revision, also inspect the V8 gitlink. A valid roll must keep the two representations aligned; review and CI then test the new integration.'],
      lab:{title:'Trace a V8 roll without changing your checkout',aim:'Identify the exact revision recorded in DEPS and the submodule representation.',prediction:'The V8 revision is expressed through a DEPS variable, while a Git gitlink stores a commit directly.',steps:['Find v8_revision and the src/v8 dependency expression in src/DEPS.','Inspect src/.gitmodules for the v8 section and read the git_dependencies mode.','Read the getdep and setdep code in depot_tools/gclient.py; locate where the tool updates a Git index entry.','Write the expected two-file diff for a roll to a hypothetical new V8 revision.'],observation:'The DEPS edit changes the revision variable; a synchronized submodule roll also changes the mode-160000 gitlink.',conclusion:'A dependency update is a coordinated state change, not merely a text replacement.',follow:'Which presubmit or CI check detects disagreement between the DEPS pin and gitlink?'},
      files:[source('DEPS','V8 pin and git dependency mode'),source('.gitmodules','Submodule paths and conditions'),source('gclient.py','getdep and setdep','depot'),source('gclient_eval.py','Token-preserving DEPS editing','depot'),source('gclient_scm.py','Git checkout and submodule behavior','depot')]
    },
    remote: {
      id:'remote', part:'Source and build', title:'How Chromium compiles a large checkout',
      deck:'Understand remote execution, Siso, compiler inputs, and build-cache failures.', deps:['checkout','rolls'], skill:'Build execution and caching',
      lead:'Once dependencies are on disk, Chromium still has to execute a very large build graph. A remote build can send many compile actions to other machines, but only when each action describes its inputs precisely. The studied tree uses Siso for much of that work and retains some reclient plumbing.',
      flow:['GN action','Find inputs','Action digest','Remote cache or execution','Output'],
      sections:[
        ['A compile command is not its complete input',`<p>A C++ compile action depends on the source file, included headers, compiler binary, flags, generated files, and sometimes environment or platform details. A remote machine cannot run it correctly if one of those inputs exists only on your workstation. A cache cannot safely reuse an output if two semantically different actions have the same identity. The hard engineering problem is discovering and naming every input, not merely opening a network connection to a worker.</p><p>That is why dependency scanning mattered in Goma and still matters in later tools. Goma’s client scanned preprocessor directives to find headers before upload; the supplied research traces its include processor and cache. Its particular client and protocol are historical at this revision, but the input-discovery problem remains. When a remote build mysteriously fails on only one machine, ask whether an undeclared header, generated file, tool version, or host property changed the action.</p>`],
        ['What remote execution stores',`<p>Reclient and Siso can use the Remote Execution API. Inputs are stored in a content-addressed store: a digest identifies file contents, and directory messages form a tree whose root digest identifies an input snapshot. An action identifies a command and that input root. The remote service can look for an existing action result or execute the action and store its outputs. This gives a precise way to reuse work when inputs are identical.</p><p>Content addressing has a cost. Scanning and hashing a very large tree consumes local work, and uploading uncached blobs consumes bandwidth. It also has a benefit: a cache hit is based on declared content rather than a file timestamp that happens to look unchanged. The cache is only as correct as the action description. A hidden dependency can make a reproducible-looking cache return the wrong result.</p><div class="code-path"><b>Remote action identity</b><code>command + flags + platform + input-tree digest → action digest → cached result or remote execution</code></div>`],
        ['Where Siso is selected',`<p><code>build/toolchain/siso.gni</code> computes a default for <code>use_siso</code>. In the studied checkout it checks for a configured <code>.sisoenv</code>, <code>build_with_chromium</code>, and the absence of an existing Ninja <code>.ninja_deps</code> in the output directory. The last condition avoids silently changing build tools inside an already populated output directory. A local <code>args.gn</code> can override the default. <code>build/toolchain/rbe.gni</code> then connects <code>use_remoteexec</code>, <code>use_siso</code>, and <code>use_reclient</code>.</p><p>Do not infer that using Siso always means remote execution. Siso can schedule local actions, and <code>use_remoteexec</code> controls whether configured rules use a remote backend. The generated <code>.sisoenv</code> and GN log are inputs to this decision. Read those files in the exact output directory you are debugging.</p>`],
        ['Why the tools changed',`<p>Goma intercepted compiler commands through a client and daemon, discovered inputs, uploaded them, and asked a backend to compile. Its custom transport became costly to maintain. Reclient adopted the standard Remote Execution API while retaining a wrapper/proxy shape; Siso integrates build scheduling and remote execution more directly. The pinned tree still contains reclient code and configuration for particular consumers. A legacy file’s presence does not prove it is on your build’s active path.</p><p>These generations make a useful design comparison. A wrapper can add remote execution to an existing build tool with limited disruption. A more integrated scheduler can see the build graph and filesystem state directly, but it requires more configuration and migration work. The standard protocol allows clients to share remote infrastructure. The choice is about where the system models dependencies and scheduling, not only about network transport.</p>`],
        ['Debug a remote-only failure',`<p>First reproduce with a single action if possible. Record the command, compiler revision, platform properties, and action/input digests. Compare remote and local execution under the same inputs. If remote reports a missing file, trace who should have declared it. If output differs while digests match, investigate nondeterminism, environment, and tool identity. If a cache hit returns stale output, inspect the declared input tree before blaming the cache server.</p><p>For a practical experiment, build one small target locally and with remote execution enabled, keeping the same source revision and GN args. Inspect Siso logs for cache hit, remote execution, or local fallback. Change one included header and predict which action digest and downstream outputs should change. A result that fails to change exposes a missing dependency.</p>`]
      ],
      quiz:['A C++ file is unchanged, but an included generated header changed. May a remote cache reuse the old object file?', 'No. The header must be part of the action input tree, so the action identity changes. If it is missing from the declared inputs, the cache may return an incorrect result.'],
      lab:{title:'Trace one compile action',aim:'Find what a remote compile action needs and how Siso is selected.',prediction:'Changing an included header should invalidate the relevant compile action even if its .cc file is unchanged.',steps:['Read build/toolchain/siso.gni and build/toolchain/rbe.gni; write the conditions for Siso and remote execution.','Choose one small C++ target and list its source, headers, generated files, compiler, and flags.','With a build available, compare one local and one remote Siso run; record cache status and action inputs.','Change one included header and predict the compile action that must rerun.'],observation:'The action should rerun or miss cache when a declared input changes; logs should show the selected execution path.',conclusion:'Remote build correctness depends on an accurate action input graph.',follow:'Which undeclared input would make this action appear nondeterministic?'},
      files:[source('build/toolchain/siso.gni','Siso selection'),source('build/toolchain/rbe.gni','Remote execution and reclient relationship'),source('build/toolchain/gcc_toolchain.gni','Compiler command wiring'),source('docs/linux/build_instructions.md','Local remote-build setup'),source('build/config/siso/configure_siso.py','Generated Siso environment','src',false)]
    },
    parsing: {
      id:'parsing', part:'Renderer and page', title:'How HTML and CSS become Blink objects',
      deck:'Understand generated names, HTML tree construction, CSS property IDs, and parser reentrancy.', deps:['navigation'], skill:'Parsing and generated code',
      lead:'A response body arrives as bytes and becomes a document. Blink cannot allocate a different hand-written parser branch for every HTML tag or CSS property spelling. It generates compact name and property tables, then uses them in runtime parsing and object construction.',
      flow:['Response text','HTML tokens','Tag lookup','Tree builder','DOM objects','CSS property IDs'],
      sections:[
        ['The input is text; the implementation uses IDs',`<p>The parser receives tag and attribute names as text. Repeated string comparisons would be expensive and error-prone, so Blink generates a known set of names from <code>core/html/html_tag_names.json5</code>. A generated lookup turns a tag spelling into an <code>HTMLTag</code> value and a qualified name. The generated declarations are build outputs; you may not find them as checked-in headers.</p><p>CSS follows a related pattern. <code>core/css/css_properties.json5</code> describes property names and metadata. Generators produce compact <code>CSSPropertyID</code> values and lookup code. A property ID makes later style code work with a small integer and structured metadata instead of reparsing the name each time. This is a representation choice: pay complexity at build time to simplify and speed up common runtime paths.</p>`],
        ['The HTML path from token to element',`<p>At the studied revision, <code>HTMLTokenizer</code> consumes input and emits tokens. <code>AtomicHTMLToken</code> caches a recognized tag identity. <code>HTMLTreeBuilder</code> applies HTML insertion modes, because real HTML is not just nested XML: omitted tags and malformed nesting need specified recovery. <code>HTMLConstructionSite::CreateElement</code> turns a tree-building instruction into a Blink element.</p><p>The mapping from tag to class is not one-to-one. Heading tags can share an element class; many less specialized tags use <code>HTMLElement</code>. The important source investigation is to find the generated factory rule and any explicit <code>interfaceName</code> override for the tag you study. Counts of tags and classes in the supplied research describe the pinned revision only.</p><div class="code-path"><b>Follow one &lt;div&gt;</b><code>HTMLTokenizer → AtomicHTMLToken → generated tag lookup → HTMLTreeBuilder insertion mode → HTMLConstructionSite::CreateElement → HTMLDivElement</code></div>`],
        ['Why element construction can run script',`<p>Custom elements make creation more than allocation. If a definition exists for a tag, <code>HTMLConstructionSite::CreateElement</code> can invoke author code while the parser is still building the tree. The supplied source walk-through shows a microtask checkpoint and a <code>CEReactionsScope</code> around this path. Author code may mutate the document or trigger other work before parsing resumes.</p><p>This is reentrancy. A naïve parser might assume that the document and current parent stay unchanged between “choose insertion point” and “append element.” That assumption can fail when constructing an element calls back into JavaScript. When debugging a parser crash or unexpected DOM order, ask which step can execute script and what state the tree builder protects before crossing that boundary.</p>`],
        ['Parsing must share the main thread',`<p>Parsing competes with input, script, and rendering for renderer main-thread time. Blink therefore processes input in chunks and can yield rather than monopolizing the thread until the entire document is parsed. The source research identifies token and time budgets at the pinned revision; the exact thresholds are implementation details that may change. The stable mental model is that parsing is scheduled work and must allow other work to run.</p><p>To observe it, load a large generated HTML document with a trace running. Locate parser tasks and frame updates. Then insert a custom element whose constructor does substantial work. Predict whether the constructor delays parsing and rendering, and inspect the trace. The experiment tests a concrete cost of reentrant author code.</p>`],
        ['Where CSS parsing leads next',`<p>Recognizing <code>width</code> as a property ID does not compute an element’s width. CSS parsing creates declarations; the cascade decides which declaration wins; computed style resolves values and inheritance; layout uses computed style with a constraint space to produce geometry. The next rendering chapter begins at that boundary. Keep the intermediate representations separate when investigating a style bug: a property can be parsed correctly but lose in the cascade or be applied to a layout object whose constraints differ from your expectation.</p>`]
      ],
      quiz:['Does seeing HTMLTag::kDiv in a C++ reference mean the enum is checked into the source tree?', 'No. The name and lookup are generated from Blink’s JSON5 inputs during the build. Inspect the input, generator, and generated output for the build under study.'],
      lab:{title:'Follow one tag from data to DOM',aim:'Connect generated metadata to runtime parser behavior.',prediction:'The tag name is turned into an ID before the tree builder constructs the final element.',steps:['Open html_tag_names.json5 at the pinned upstream revision and find div or a tag with an interfaceName override.','Read the generator named make_element_factory.py and identify how it chooses a class.','Follow HTMLTokenizer, AtomicHTMLToken, HTMLTreeBuilder, and HTMLConstructionSite::CreateElement in the pinned source.','Find a web test that parses this tag; for a custom element, locate a test that runs a constructor during parsing.'],observation:'You should find a generated name-to-ID path, then a runtime tree-building path that can execute author code.',conclusion:'Generated tables solve name recognition; tree construction must still preserve HTML rules and handle reentrancy.',follow:'What parser state must remain valid if a custom element constructor changes the DOM?'},
      files:[source('third_party/blink/renderer/core/html/html_tag_names.json5','HTML tag generator input','src',false),source('third_party/blink/renderer/core/css/css_properties.json5','CSS property generator input','src',false),source('third_party/blink/renderer/build/scripts/make_element_factory.py','Element factory generator','src',false),source('third_party/blink/renderer/core/html/parser/html_tokenizer.cc','HTML tokenization','src',false),source('third_party/blink/renderer/core/html/parser/html_tree_builder.cc','Insertion modes','src',false),source('third_party/blink/renderer/core/html/parser/html_construction_site.cc','Element construction and custom-element path','src',false)]
    },
    features: {
      id:'features', part:'Web APIs and memory', title:'How Blink features are enabled and tested',
      deck:'Trace a feature from JSON5 and generated flags to contexts, web tests, and rollout.', deps:['bindings','lifetime'], skill:'Feature exposure and testing',
      lead:'Implementing a web API in C++ is only one step toward making it usable. Blink controls exposure with generated runtime feature state, execution context, platform and field configuration, and tests. A feature can exist in the source while remaining unavailable to a page.',
      flow:['Feature row','Generated state','Context check','Binding installed','Web test','Rollout'],
      sections:[
        ['A feature starts as structured data',`<p><code>platform/runtime_enabled_features.json5</code> declares feature names and fields such as status, platform-specific status, dependencies, and sometimes origin-trial information. A generator turns those rows into accessors and compact feature state. The declared status describes a default testing and exposure tier, not the complete runtime truth for every user.</p><p>At the pinned revision, the research distinguishes <code>test</code>, <code>experimental</code>, and <code>stable</code> status. A <code>test</code> feature is available to web-test runs, an <code>experimental</code> one can be enabled through the experimental web-platform switch, and a <code>stable</code> one is available by default. Per-platform dictionaries and dependencies complicate the matrix. Read the row and generated accessor for the feature you study instead of treating the status word as the whole decision.</p>`],
        ['Several gates can affect exposure',`<p>A command-line switch may enable or disable Blink features. A <code>base::Feature</code> may supply state through field configuration. A generated <code>RuntimeEnabledFeatures::XEnabled()</code> accessor applies dependencies and implied-by relationships. An origin-trial feature needs a context-aware check because exposure can depend on a signed token associated with an origin. Generated bindings then use those checks when installing a web-visible interface or member.</p><p>These are distinct questions: is the C++ implementation compiled, is a flag true, is the API installed in this execution context, and may this origin use it? A browser can answer them differently for a normal page, a test shell, an isolated world, or a trial participant. If an API is missing, test each gate in order before editing its implementation.</p><div class="code-path"><b>Debug an absent API</b><code>IDL exposure → generated binding condition → runtime feature state → context/origin check → observed JavaScript property</code></div>`],
        ['An origin trial is a context check',`<p>Origin trials allow a web feature to be enabled for a specific origin under a signed token. The token structure documented in Chromium includes a signature and payload containing origin, feature, and expiry data. A global boolean cannot express that decision: two pages in the same browser may have different trial eligibility. This is why a context-free flag accessor can be the wrong check for a trial-gated API.</p><p>The supplied documentation disagrees on the allowed duration of an initial trial. The course does not select one number. For engineering work, inspect the current policy and the code that validates the token at the exact revision you are using. The general model is an origin-scoped permission to expose an experimental feature, not a guarantee that the feature is enabled for all users.</p>`],
        ['What a web test establishes',`<p>Blink web tests run page content, often under <code>content_shell</code>, and compare observable results. The <code>web_tests/TestExpectations</code> file can mark known failures or platform-specific expectations; a listed failure does not become a passing implementation. Virtual test suites can rerun a set of tests with a different feature configuration, and WPT tests exercise web-standard behavior shared with other browsers.</p><p>When a test is green, read its setup: which runtime flags were enabled, which platform ran, whether the test used a virtual suite, and whether it was skipped elsewhere. A passing test proves that setup’s observation. It does not prove rollout to stable users or correct behavior in every origin. The source archive contains <code>docs/testing/web_tests.md</code> locally; the Blink test tree itself is absent from the sparse checkout.</p>`],
        ['From implementation to shipping',`<p>A careful feature review follows the same order as a user’s experience: IDL declaration, binding generation, runtime state, implementation, browser-side authority if needed, web test, and release branch or field rollout. A failure at one step should not be explained by a later one. For example, if the property is absent on <code>window</code>, begin with exposure and flags. If it exists but rejects a promise, inspect the implementation and service call.</p><p>For an experiment, pick a feature row with a non-stable status. In a Chromium build, run a focused web test with and without the enabling switch. Predict whether the property is present and whether the test behavior changes. Record the exact command-line flags and runtime feature state; otherwise the observation is hard to reproduce.</p>`]
      ],
      quiz:['A feature has status stable in JSON5. Does that alone prove it is exposed to a particular page?', 'No. Check platform status, dependencies, command-line or field overrides, execution context, origin-trial requirements, and generated binding exposure.'],
      lab:{title:'Investigate one gated web API',aim:'Find every gate between a source implementation and a page-visible API.',prediction:'The IDL and feature row determine whether generated bindings install the API in the test context.',steps:['Choose one named feature from runtime_enabled_features.json5.','Find the corresponding IDL declaration and any RuntimeEnabledFeatures accessor cited by its generated binding.','Read a relevant web test, including its flags, virtual suite, and TestExpectations entry if present.','Write two predictions: API exposure in a default page and in an explicitly enabled test run.'],observation:'The predictions may differ because the execution context and feature state differ.',conclusion:'Source presence, test exposure, and user rollout are separate facts.',follow:'Would an origin-trial token change the answer for only one origin?'},
      files:[source('third_party/blink/renderer/platform/runtime_enabled_features.json5','Feature declarations','src',false),source('third_party/blink/renderer/bindings/','Generated binding inputs','src',false),source('third_party/blink/web_tests/TestExpectations','Known test expectations','src',false),source('docs/testing/web_tests.md','How web tests run'),source('third_party/blink/public/common/origin_trials/origin_trials_token_structure.md','Origin-trial token structure','src',false)]
    },
    release: {
      id:'release', part:'Builds and releases', title:'How a Chromium change reaches a release',
      deck:'Separate main-branch submission, version branches, backports, and stable rollout.', deps:['factory','rolls'], skill:'Release branches and backports',
      lead:'A change submitted to main is available to future builds, but it has not automatically shipped. Chromium branches a milestone, stabilizes it, and selectively brings fixes onto release branches. Version numbers and release automation identify which branch and build contain a change.',
      flow:['Land on main','Cut branch','Stabilize','Merge fix if needed','Build and test','Roll out'],
      sections:[
        ['Main and branch answer different questions',`<p>The commit queue lands a reviewed change on <code>refs/heads/main</code>. A milestone branch is a separate line of development cut from main. After the cut, a new main-branch commit does not appear on that branch unless it is merged. To answer “does this fix ship in version X?”, first identify the release branch, then check whether the commit or an equivalent backport is on it.</p><p>The checked-in <code>docs/process/release_cycle.md</code> describes the cadence at <code>src@f288fed6</code>. It says milestones develop on main for two weeks before branching, then stabilize before stable release. This is a revision-specific process statement, not a permanent rule. It also distinguishes Beta, Stable, Extended Stable, and refreshes. Release schedules can change independently of the architecture taught here.</p>`],
        ['What the version fields identify',`<p><code>chrome/VERSION</code> contains <code>MAJOR</code>, <code>MINOR</code>, <code>BUILD</code>, and <code>PATCH</code>. In the pinned tree it reads 156.0.8067.0. The research connects the third field to a branch-head number, and the fourth field advances on a branch as builds are made. A version string is therefore a clue to branch identity, not just a marketing label.</p><p>Do not infer inclusion from the major number alone. Find the exact branch-head ref and commit range. A fix may be on main, on one branch, or on several branches at different commit hashes. When comparing crash reports, record full version and channel, then map them to source revision and build configuration.</p>`],
        ['How a fix moves to a branch',`<p>A branch merge is a new integration decision. The change may need a cherry-pick, conflict resolution, review, and testing against the older branch’s dependencies. The branch may have different V8 or third-party pins from main, so a clean main build does not prove the backport is safe. The supplied CI research shows branch-specific configuration generated from <code>infra/config</code> and milestone metadata.</p><p>For an investigation, choose a known fix and build a table with main commit, branch commit, branch number, dependency pins, and the test result that validates the merge. If the branch commit is absent, state that the fix is not established as shipped on that branch. Avoid treating a release note or a passing main tryjob as proof of branch inclusion.</p>`],
        ['What the V8 release scripts illustrate',`<p>The separate V8 checkout includes <code>tools/release/create_release.py</code> and <code>common_includes.py</code>. Their step-based scripts read version fields, prepare a branch and tag, save state, and can resume after a failed step. A last-known-good ref chooses a candidate before the release script acts. The automation divides responsibilities: CI determines a good source revision; a release script turns it into a versioned artifact; an autoroller can then bring that V8 version into Chromium.</p><p>This is an example of a broader engineering pattern: long workflows need durable state and explicit checkpoints. If a push fails after several steps, a resumable script should know what has already happened rather than blindly repeating every mutation. The V8 checkout reports a different revision from Chromium’s pinned V8, so treat this script as a concrete nearby implementation, not exact proof of Chromium’s pinned V8 contents.</p>`],
        ['A release question you can actually answer',`<p>Ask: “Does the stable build with version A.B.C.D include fix F?” First map <code>C</code> to a branch head. Then search that branch for F or its backport, inspect the build’s source revision and DEPS pins, and verify the affected feature is enabled in that channel. Finally check rollout if the question is about users rather than binaries. Each step narrows a different uncertainty.</p><div class="code-path"><b>Evidence chain</b><code>Fix commit → branch inclusion → build revision → dependency graph → feature state → rollout</code></div><p>A gap in the chain is a clear next task, not a reason to guess.</p>`]
      ],
      quiz:['A fix landed on main after milestone M branched. Is it in M stable?', 'Not without evidence of a branch merge and a build containing it. Check the branch-head history and full build revision.'],
      lab:{title:'Trace a fix to a release branch',aim:'Practice proving whether code is included in a particular build.',prediction:'A main-branch commit after the cut will need a separate branch merge to appear in that milestone.',steps:['Read docs/process/release_cycle.md and chrome/VERSION at the pinned revision.','Choose a fix described in source history and identify its main commit.','Locate the relevant branch-head and search for the fix or backport.','Record the branch version, build revision, and what evidence would establish rollout to users.'],observation:'The branch history may contain a different commit hash for the same fix, or no merge at all.',conclusion:'Landing, branch inclusion, build inclusion, and rollout are separate states.',follow:'Did the branch use the same dependency revisions and feature flags as main?'},
      files:[source('docs/process/release_cycle.md','Pinned release process'),source('chrome/VERSION','Version fields'),source('infra/config/milestones.json','Milestone branch metadata'),source('tools/release/create_release.py','V8 release steps','v8'),source('tools/release/common_includes.py','V8 resumable step framework','v8')]
    }
  };
}
