export const outcomes = {
  evidence:['Distinguish what a document, implementation, test, and runtime observation establish.','Turn an uncertain explanation into a concrete source search or experiment.'],
  checkout:['Explain why a src revision alone does not reproduce a build.','Trace a dependency from DEPS evaluation through checkout and generated GN settings.'],
  rolls:['Identify every representation a dependency roll must update.','Separate a child-project regression from a Chromium integration failure.'],
  remote:['List the inputs that determine a remote compile action.','Explain how an undeclared input can invalidate cache correctness.'],
  owners:['Place WebContents, frame hosts, documents, Blink, cc, and Viz in their owning processes.','Explain why a tab, frame, site, and renderer process are different identities.'],
  startup:['Separate process management state from child-process readiness.','Find which initialization must happen before a sandboxed role can run.'],
  ipc:['Follow a Mojo endpoint from creation through binding, dispatch, and reply.','Identify the authority, lifetime, and disconnect behavior of an interface.'],
  navigation:['Distinguish requested URL, committed document, and visible page.','Explain which state survives cancellation, commit, and renderer failure.'],
  security:['Authorize an operation using trusted origin and process state.','Explain the separate roles of site isolation, sandboxing, and IPC validation.'],
  network:['Locate a failure before commit, during resource loading, or after bytes arrive.','Explain how a browser-created loader factory constrains a renderer request.'],
  parsing:['Follow an HTML or CSS name through generated tables to runtime objects.','Explain why script execution during element construction requires reentrancy checks.'],
  time:['Follow a callback across posting, queueing, and execution.','Distinguish task priority, microtask checkpoints, and rendering opportunities.'],
  pixels:['Identify the first rendering stage affected by a page change.','Distinguish paint records, compositor trees, raster output, and displayed frames.'],
  bindings:['Trace a JavaScript API from IDL through generated bindings to Blink.','Explain how contexts, worlds, wrappers, and feature gates affect exposure.'],
  lifetime:['Draw strong, weak, and rooted edges in a Blink object graph.','Distinguish memory reachability from permission to act on the current document.'],
  storage:['Separate live document state, BFCache retention, origin storage, and profile files.','Describe what a data-format change must preserve across browser versions.'],
  features:['Find the gates between compiled implementation and page-visible API.','State exactly what a passing web test proves about exposure and rollout.'],
  graphs:['Explain how value, control, and effect edges constrain optimization.','Check that a graph transformation preserves branch/value pairing and memory order.'],
  factory:['Follow authored configuration through a build to its test tasks.','Use retry and baseline evidence to evaluate whether a patch caused a failure.'],
  release:['Prove branch and build inclusion separately from main-branch landing.','Explain why feature configuration and rollout still matter after inclusion.'],
  diagnostics:['Choose a trace, CPU profile, or memory measurement that can test a hypothesis.','Design a before/after comparison and state what could confound its result.'],
  journey:['Follow one page load across process, policy, loading, and rendering boundaries.','Pair each transition with a source path, observation, and possible failure.'],
  independent:['Produce a reviewable investigation of an unfamiliar subsystem.','Name the evidence that would disprove your model and run the next experiment.']
};

// Worked reasoning problems use hypothetical observations. They do not claim
// that the experiments were run against the supplied sparse checkout.
export const reviewProblems = {
  remote:{title:'An unchanged source file produces a different object file',problem:'Two machines compile the same .cc file at the same src revision. One reuses a cached object; the other compiles and gets a different result. A generated header changed on only one machine. What must you compare before deciding which result is correct?',answer:'Compare the evaluated dependency graph, generated header contents, compiler and flags, then the declared input tree and action digest. If the generated header affects compilation, the two actions are different even though the .cc file is unchanged. If their digests still match, the input declaration may be incomplete. A cache hit alone cannot validate the result; it only reports a match under the declared identity.',experiment:'Use an isolated build output directory. Change one generated input in a controlled build and inspect whether the action identity and output change. Keep toolchain and flags fixed. A clean rebuild helps distinguish stale output from source behavior.'},
  ipc:{title:'A response arrives after the document that asked for it is gone',problem:'Document A requests browser work. The user navigates to B before the service responds. The callback target is still alive. Is invoking the callback safe?',answer:'Reachability answers only whether the object can still be accessed. Determine whether the receiver and callback are scoped to A, whether navigation cancels them, and whether the result can affect B. A strong reference may prevent a memory error while preserving the wrong authority. The correct behavior depends on the API contract: drop the obsolete work, report cancellation, or deliver only to a still-valid owner.',experiment:'In an existing test fixture, delay a fake Mojo response until after navigation. Assert which callback, document, or UI state changes. Also test disconnect. A success-only test does not exercise the lifetime boundary.'},
  time:{title:'A higher-priority task still waits 200 milliseconds',problem:'An input task is posted while JavaScript runs for 200 ms on the same renderer main thread. Raising the input queue priority does not reduce the wait. Why can this be correct?',answer:'Ordinary task priority chooses the next eligible task; it does not preempt a running JavaScript callback. The input can remain queued until the current task yields. Check when the input was posted, when the long task ended, and when the scheduler selected the input task. If the wait lies entirely inside the running task, queue selection is not the immediate cause.',experiment:'Compare the original long task with the same work split into smaller tasks. Record both input latency and total work duration. Improving responsiveness may add scheduling overhead; measure both instead of claiming the split is free.'},
  pixels:{title:'The DOM changed, but the displayed frame did not',problem:'A trace confirms a DOM mutation, and computed style shows the expected color. The screen still displays the old color. Which evidence would narrow the investigation next?',answer:'The DOM and style observations place the earliest known correct state before paint. Check paint invalidation and recording, compositor commit, raster readiness, activation, and display. Geometry may be unchanged, so absence of layout work is not itself a bug. Find the first stage that lacks the new visual state. Only then inspect that stage’s scheduling and ownership.',experiment:'Change a single element’s color in a minimal page. Compare style, paint, and compositor events with a baseline. Then change its size and compare the additional layout work. These two changes help separate geometry from appearance.'},
  security:{title:'A valid URL is the wrong authorization input',problem:'A privileged method allows access when a frame’s URL matches a trusted site. The frame can be sandboxed and have an opaque origin. The URL parser and Mojo message validation both pass. What is still wrong?',answer:'Neither check establishes the frame’s security identity. A normal-looking URL can belong to an opaque-origin document. The receiver should use browser-known committed origin and the operation’s policy, or validate a necessary claim against trusted context. Reconstructing authority from an address can discard exactly the information that distinguishes these cases.',experiment:'Find an existing browser test covering inherited or opaque origins. Compare its URL and committed-origin assertions. Add a focused authorization case in the appropriate fixture, and verify the expected rejection on the privileged side.'},
  factory:{title:'A failing tryjob passes on its second run',problem:'The same test fails with a patch, then passes once on retry. A teammate concludes the patch is innocent. What does the result actually establish?',answer:'It establishes that the failure is not reproduced in every observed run. The patch can still introduce a timing-sensitive regression. Compare the same shard configuration, baseline results, test variants, and stability history. A failure without the patch weakens attribution to the patch; repeated failures only with it strengthen attribution. Neither one retry nor one baseline run proves a universal conclusion.',experiment:'Write the attribution table before re-running: patched or baseline, shard, binary identity, environment, and outcome. Preserve failing logs. Change test isolation only as a deliberate experiment because it may remove the state that triggers the failure.'},
  journey:{title:'One visual symptom, two different causes',problem:'In one run a stylesheet request fails. In another it succeeds, but a long script task delays the update. Both runs briefly show the wrong appearance. How would you distinguish them without changing the browser implementation?',answer:'Record the stylesheet request result, computed style, and renderer timeline. A failed request prevents its rules from becoming input to style. A successful request with a busy main thread can leave style or later rendering queued. Compare the first divergence from a baseline: request completion, rule availability, style update, or a later rendering stage. The screenshot alone does not identify the cause.',experiment:'Use the small page-load exercise below. First break only the stylesheet URL; then restore it and add only the long task. Pair each observed difference with the stage that should explain it, and state any stage you could not observe.'}
};
