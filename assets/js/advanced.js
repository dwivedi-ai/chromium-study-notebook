// Focused walkthroughs added after a second coverage and accuracy audit.
export const advancedSections = {
  checkout: [
    ['What a CI checkout actually does', `<p>A CI builder does not simply clone <code>src</code> and run a compiler. Its recipe calls <code>bot_update</code>, which materializes a specific patch or commit and its dependency graph. The recipe module prepares inputs and calls a resource script; that script uses depot_tools and a Git cache. Hooks run after the source checkout. A failure in <code>bot_update</code> may mean a missing revision in the cache, a fetch failure, a malformed DEPS file, or a patch that cannot be applied. A failure in <code>gclient runhooks</code> is a later problem.</p><p>This explains why a bot can be fast with a warm Git cache and slow with a cold one without testing different code. It also explains why reproducing a CI failure starts with the bot’s reported checkout revisions and generated build arguments, not just the Gerrit patch number. The bot_update recipe and its resource script live in depot_tools’ recipe modules; they are absent from this sparse copy, so inspect the pinned upstream source for their exact command construction.</p>`]
  ],
  time: [
    ['A more precise microtask rule', `<p>The convenient rule “microtasks run after a task” is incomplete for Blink. The scheduler’s <code>EventLoop</code> mainly manages a microtask queue; ordinary task queues are attached to frames so they can be throttled, frozen, and prioritized separately. A Blink agent owns that event loop. V8 can perform a checkpoint when an outermost script scope exits, and the main-thread scheduler performs a checkpoint at task completion. A microtask can therefore run between two browser-dispatched event listeners when each listener leaves its outermost script scope.</p><p>To test the timing, register two listeners for one click. In the first, log A and queue a promise reaction that logs B; in the second, log C. Predict the order, run the page, then inspect the event dispatch and script-scope path that explains it. Do not generalize the result to every possible nested script call: checkpoint timing depends on scope and reentrancy. The systems lesson is to locate the exact boundary where deferred work is drained, not to memorize a slogan about the event loop.</p>`]
  ],
  pixels: [
    ['How style avoids testing every selector', `<p>Matching every CSS selector against every element would be wasteful. Blink indexes rules by useful parts of their rightmost compound selector. A selector such as <code>p.notice</code> can be considered from the <code>notice</code> class bucket instead of from the entire stylesheet. Candidate rules are then checked against the element, and the cascade selects the winning declarations. The output is an immutable <code>ComputedStyle</code>.</p><p>Invalidation is related but different. When a class changes, Blink needs to identify which elements might have a different computed style. It can conservatively mark more elements than strictly necessary; correctness requires not missing affected ones, while performance benefits from keeping the set small. If a page unexpectedly restyles thousands of elements, inspect the selector structure and invalidation sets before blaming layout. The detailed implementation lives under Blink’s <code>core/css</code> tree and is cited by the supplied research but absent locally.</p>`],
    ['The lifecycle is an enforced state machine', `<p>Blink’s document lifecycle records states such as style clean, layout clean, pre-paint clean, and paint clean. Code in a later stage can assert that earlier work reached the required state. The point is more than naming pipeline stages: it makes an invalid call order visible during development. A style mutation moves the document back toward a pending visual update; the next frame advances it through the required stages.</p><p>Do not infer from a state name that every stage repeats fully. Incremental style invalidation, layout caches, and paint invalidation can reuse previous immutable results. When a trace shows a visual update, check both the lifecycle transition and how much work each stage performed. The current header is <code>third_party/blink/renderer/core/dom/document_lifecycle.h</code>; older layout documentation in the source archive uses state names that the studied header no longer has.</p>`]
  ],
  bindings: [
    ['What happens before a generated binding exists', `<p>Blink’s Web IDL compiler has a middle representation, not just a direct IDL-to-C++ template. It first collects IDL syntax trees, builds a database that merges partial interfaces and mixins, resolves inheritance and overload groups, and calculates exposure. Generators then emit V8 binding code. This phase order matters when two IDL files seem to define one interface: looking at only one declaration can hide methods mixed in elsewhere.</p><p>For a method with overloads, inspect the combined IDL database or generated binding in a built tree. The generated code decides which overload applies and converts JavaScript arguments before calling Blink C++. If a call throws a type error before reaching your implementation breakpoint, the bug may be in the web contract or invocation, not the implementation body. The source research cites <code>collect_idl_files.py</code>, <code>build_web_idl_database.py</code>, and <code>generate_bindings.py</code> in Blink’s bindings scripts.</p>`],
    ['From a script element to running JavaScript', `<p>A script element does not immediately become an arbitrary C++ call. Blink prepares it through <code>ScriptLoader::PrepareScript</code>, waits for the appropriate inline or external script readiness, and ultimately reaches <code>PendingScript::ExecuteScriptBlockInternal</code> and <code>V8ScriptRunner::CompileAndRunScript</code>. Module scripts, streaming compilation, caching, and parser blocking introduce branches in this path. The surrounding document and execution world determine the V8 context.</p><p>When debugging “my script ran too early,” locate the parser state, script kind, load completion, and task or microtask boundary. A breakpoint in <code>V8ScriptRunner</code> proves execution reached V8; it does not explain why the script became ready at that time. Follow the preparation and pending-script object as well. The named implementation files are upstream investigation targets because Blink’s implementation is missing from this sparse local checkout.</p>`]
  ],
  factory: [
    ['Expand one builder instead of memorizing the fleet', `<p>Consider the studied <code>ci/Linux Builder</code>. Its Starlark declaration selects a builder spec, GN argument bundle, compile targets, and contact information. Defaults at the Linux group and bucket levels supply pool, OS, CPU, trigger, and timeout values. <code>lucicfg</code> expands this authored configuration into the generated Buildbucket entry. At runtime a bootstrapper starts the recipe; the builder declaration is configuration, not a shell script containing every command.</p><p>To investigate another builder, follow the same chain: declaration → inherited defaults → generated config → recipe properties → one actual build record. If the build runs on the wrong pool, look at inherited configuration. If it compiles the wrong target, inspect its target bundle and generated GN args. If it runs a surprising step, inspect the recipe version and build properties. This chain is more useful than searching for the builder’s name in every file.</p>`],
    ['Why the orchestrator and compilator are separate', `<p>Some commit-queue jobs use an orchestrator build that remains the user-facing result while a compilator build handles checkout and compilation on a larger machine. The orchestrator can wait on a smaller machine; the compilator releases its expensive machine after it produces outputs and runs checks that need the full tree. Tests can then run as separate Swarming tasks. <code>docs/infra/builder_types.md</code> describes this pattern and its predecessors.</p><p>The split is an allocation decision. One large machine held idle through hundreds of test shards wastes capacity. A pure builder/tester split can create too many top-level results for a developer to inspect. The orchestrator retains one coherent result while expensive work runs where it belongs. When a build appears “stuck,” find whether it is waiting for compilation, a Swarming shard, or result processing before assuming the orchestrator process itself is slow.</p>`],
    ['Read a CQ failure as a sequence of comparisons', `<p>The pinned <code>docs/infra/cq.md</code> describes a four-part test path: compile affected suites, run shards with the patch, retry failing shards with the same configuration, then run remaining failing suites without the patch. A failure that disappears on retry is not treated like a reproducible regression. A failure that also occurs without the patch is evidence against blaming the change. LUCI Analysis and ResultDB can record further exoneration for known unstable or noncritical results.</p><div class="code-path"><b>Attribution question</b><code>Failed with patch? → Failed again with patch? → Passed without patch? → Check stability records</code></div><p>That logic consumes substantial compute, but it protects developers from a false verdict in a large flaky test fleet. For one red test, collect the original shard, retry shard, without-patch run, exact test variant, and exoneration reason. A summary badge is the end of an attribution process, not its evidence.</p>`],
    ['Post-submit failures have different owners', `<p>After submission, scheduler triggers and parent recipes fan work out to CI builders and testers. Some failures close the tree; others are infra failures or known test instability. Gardeners review breakages, and bisection can narrow a culprit range. The configuration for notifiers is in <code>infra/config/notifiers.star</code>; a failed checkout step can be classified as an infrastructure failure and need not close the tree merely because its name appears in a tree-closing expression.</p><p>When a post-submit failure appears, ask who owns the next decision: the test author, builder owner, gardener, infra on-call, or release manager. Then gather the revision range, failed step category, test history, and whether the failure reproduces locally. The broader engineering lesson is that observability and human response are part of a CI design. Automated labels alone do not repair the tree.</p>`]
  ]
};
