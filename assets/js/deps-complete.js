export const depsComplete = {
  checkout: [
    ['A checkout is a graph of three dependency types', `<p>At <code>src@f288fed6</code>, the research parsed <code>src/DEPS</code> and found 451 declared entries: 273 Git repositories, 147 CIPD entries containing 153 packages, and 31 GCS entries containing 87 objects. These are declarations, not the set every host fetches. Conditions select a subset for a particular operating system, CPU, and checkout configuration. The file also contains variables, hooks, recursive DEPS declarations, and host restrictions. A developer needs <code>src</code> plus that evaluated graph to build.</p><p>Each transport serves a different job. Git pins source histories; CIPD pins packaged tools or data by package version; GCS entries pin large objects using hashes and metadata. One manifest lets a Chromium revision identify all three. It also creates a trust obligation: conditions and variables must be parsed predictably and hooks must be understood before running them. To diagnose “same src commit, different checkout,” compare the evaluated condition inputs as well as the raw DEPS file.</p>`],
    ['What fetch, gclient, and the three local files own', `<p><code>fetch chromium</code> is an initial-checkout recipe from depot_tools. It creates a <code>.gclient</code> solution pointing at Chromium’s <code>src</code> repository, then invokes <code>gclient sync</code>. A developer normally uses <code>gclient sync</code> thereafter. <code>.gclient</code> is local checkout configuration, <code>src/DEPS</code> belongs to the project revision, and <code>.gclient_entries</code> records paths that gclient managed previously. Confusing these owners leads to hard-to-reproduce changes: editing a local custom variable does not change the project’s pin.</p><p>depot_tools also supplies command wrappers. The <code>gn</code> wrapper locates the pinned GN binary rather than being GN itself. <code>autoninja</code> and Siso wrappers similarly connect local commands to downloaded tools. The wrappers are operational glue; the pinned binary version comes from the dependency graph. On a CI bot, a recipe’s <code>bot_update</code> path materializes the checkout instead of running the developer-facing <code>fetch</code> command.</p>`],
    ['How the restricted DEPS grammar protects the reader', `<p><code>gclient_eval.Exec</code> parses DEPS into a Python syntax tree but accepts only a narrow language. Top-level assignments must target simple names. Allowed values include literals, lists, dictionaries, limited concatenation and formatting, and the <code>Var</code> and <code>Str</code> helpers. Imports, function definitions, and arbitrary calls are rejected. A separate condition evaluator checks host and configuration expressions. The parser also rejects unsafe formatter field traversal that could reach Python internals.</p><p>This is not a claim that checkout is side-effect free: configured hooks later execute programs. It is a narrower claim that reading the dependency declaration is not the same as executing arbitrary Python in DEPS. If you review a change to the parser, test both positive inputs and rejected syntax; the rejected cases define the security and reproducibility boundary as clearly as the accepted ones.</p>`],
    ['The sync queue and its epilogue', `<p><code>gclient sync</code> loads the local solution, evaluates its dependency graph, and schedules eligible nodes. A child DEPS file cannot be read until its parent checkout exists, and checkouts sharing a repository URL must be coordinated. Git, CIPD, and GCS nodes can otherwise make progress in parallel. After the work queue drains, gclient performs post-sync work such as writing managed entries and generated GN arguments and running configured hooks.</p><p>The ordering is a debugging map. A parser error occurs before network work. A missing Git revision belongs to a checkout node. A hook failure can happen after all repositories are present. A stale <code>gclient_args.gni</code> can produce a build difference even when source files match. Rather than rerunning all of sync blindly, identify the first failed stage and the exact dependency path involved.</p>`],
    ['Compiler versions are part of the checkout', `<p>Chromium’s Clang revision is named in <code>tools/clang/scripts/update.py</code>, while the compiler archive is a pinned GCS dependency. GN checks that the installed toolchain stamp agrees with the expected revision. This makes a toolchain roll a source-controlled change rather than a developer’s local package upgrade. Sysroots and other host tools follow related pinning patterns through DEPS and CIPD.</p><p>When a C++ failure appears only on CI, compare compiler binary, revision stamp, sysroot, GN arguments, and build platform before changing the code. A different compiler can change warnings, generated code, link behavior, and optimization. The engineering principle is to include tools in the build input graph, not merely source files.</p>`]
  ],
  rolls: [
    ['How an automated roll remains reviewable', `<p><code>gclient setdep</code> keeps a syntax tree and the original token stream. It uses the tree to identify a revision variable or URL string, then changes that token while preserving neighboring comments and formatting. When Git dependencies are synchronized with submodules, it also updates the Git index entry of mode <code>160000</code>. The resulting review can show one DEPS line and one gitlink change instead of a reformatted manifest.</p><p>Skia’s AutoRoll service is a separate implementation. It selects a child revision, edits the parent’s pin, uploads a Gerrit change, and runs the configured commit-queue jobs. The research measured 940 machine-owned commits among 4,513 main-branch commits in one September 2026 week. That measurement shows why small and predictable diffs matter, but it should not be treated as a permanent rate. A bot can integrate continuously only if its changes are bounded and failures can be attributed and reverted.</p>`],
    ['The submodule migration is a two-representation state', `<p>Chromium’s 2023 plan proposed moving Git dependency truth toward native submodules. At the studied revision the tree is in <code>git_dependencies = 'SYNC'</code>: DEPS entries, <code>.gitmodules</code> sections, and Git gitlinks agree for the synchronized dependencies. Native Git does not understand <code>gclient-condition</code> in <code>.gitmodules</code>, and CIPD is still a DEPS concern. The later announced stages were not established as complete by the collected research.</p><p>This is a general migration pattern. A new representation can land before its old readers disappear. A tool that edits only DEPS or only a gitlink can now create an inconsistent checkout. Before changing the authoritative source, inventory every consumer: rollers, build recipes, code search, presubmit, and local developer commands. Compatibility work can dominate the timeline even after the data format itself is correct.</p>`]
  ],
  remote: [
    ['The useful idea that survived Goma', `<p>Goma intercepted compiler invocations with a wrapper and a local proxy. Its task state progressed through setup, file request, remote execution, and file response, with local execution or fallback paths. Setup was difficult because the client needed to know which headers the compiler would read under macros, include guards, <code>#include_next</code>, and conditional directives. Goma implemented an include processor and caches to avoid rescanning everything on every action.</p><p>The particular transport was custom and later replaced by a standard remote-execution stack. The input-discovery problem did not disappear. Reclient and Siso still need an accurate action input tree before the remote worker can compile correctly. That is why a hidden generated header can cause a remote-only failure even if the compiler command line looks identical. The right debugging unit is the action with all of its declared inputs, not the <code>.cc</code> filename alone.</p>`],
    ['Which build switch actually selects a remote action', `<p>The studied <code>siso.gni</code> computes a default from the configured <code>.sisoenv</code>, <code>build_with_chromium</code>, and whether an existing Ninja dependency file is present. <code>rbe.gni</code> derives <code>use_reclient</code> from <code>use_remoteexec</code> and <code>use_siso</code>. Siso itself can schedule local work. Its rule configuration reads logged GN state to decide which actions should execute remotely. A local <code>args.gn</code> override and generated checkout settings can change the path.</p><p>For a failing build, record <code>use_siso</code>, <code>use_remoteexec</code>, <code>use_reclient</code>, <code>gn_logs.txt</code>, the active Siso configuration, and the action log. “Siso was installed” does not prove that the failing action went to the remote service. Likewise, the presence of reclient code does not prove that it wrapped this compile. At the pinned revision reclient remained for particular consumers and configuration, while Siso was the main supported path.</p>`],
    ['Why a cache hit is a technical claim', `<p>Under the Remote Execution API, a command and its input tree identify an action. Content-addressed storage names blobs by digest; the action cache can reuse an output only for the matching action identity. A strong cache key is more than a hash of command text. It must include the compiler, relevant environment and platform, generated files, headers, and any inputs the action reads.</p><p>If two nominally identical actions produce different output, look for nondeterminism or an undeclared input. If a harmless path difference destroys cache reuse, inspect how paths enter the input tree. The supplied Siso research found rules that choose different platforms and timeouts for compiles, links, and generators, plus local-only exceptions. Those are part of the action description. Cache performance and cache correctness both follow from representing the build accurately.</p>`]
  ]
};
