// Extended, source-grounded chapter text. Paths without a local snapshot are
// named as investigation targets at the pinned Chromium revision.
export const deepSections = {
  evidence: [
    ['Read claims at the right level', `<p>A useful source explanation has a narrow claim. “Chromium uses processes for security” is a design claim. “This navigation selected this renderer” is a claim about one run. The first can be supported by <code>docs/process_model_and_site_isolation.md</code>; the second needs the process-selection code, its inputs, and often a trace. A source file can prove which branch exists at this revision. It cannot prove which branch ran without the runtime conditions.</p><p>Keep four columns in your notebook: <strong>claim</strong>, <strong>evidence</strong>, <strong>revision</strong>, and <strong>what would disprove it</strong>. If a document and the implementation disagree, locate the exact code path and a test before choosing one. Chromium documents can lag behind the implementation; code can also be guarded by flags or platform conditions.</p><div class="study-example"><b>Example</b><p>“A hidden page freezes after five minutes” appears in an older scheduler document. The studied implementation has a different delay under a feature flag. The honest conclusion is conditional: identify the flag, its default, and the code that reads it before giving a number.</p></div>`],
    ['Find a path through a large tree', `<p>Search by behavior before searching by class name. For a navigation, start with <code>docs/navigation.md</code>, then locate <code>NavigationRequest</code>, its callers, and tests. For a web API, start with the <code>.idl</code> declaration, then the generated binding convention, C++ implementation, and web test. For a build setting, follow <code>DEPS</code> or <code>args.gn</code> into the imported <code>.gni</code> and the target that reads it.</p><pre><code>rg --files chromium-ref/src/content | rg 'navigation_request|navigation.*test'
rg -n 'BeginNavigation|CommitNavigation' chromium-ref/src/docs chromium-ref/src/content
git -C chromium-ref/src log --oneline -- docs/navigation.md</code></pre><p>Search results are candidates, not a call graph. Check the declaration, the concrete implementation, and all places where an interface is implemented. At a task post or Mojo call, stop and draw a new arrow: the next frame of execution may be on another thread or in another process.</p>`],
    ['Use tests as executable questions', `<p>A test tells you which behavior the project currently cares to preserve. Its name is only a hint. Read setup, inputs, assertions, and any platform condition. A browser test may construct several processes; a unit test may replace the real service with a fake. That difference changes what the test can establish.</p><p>Try this method on <code>cc/scheduler/scheduler_state_machine_unittest.cc</code>: find one test about beginning a frame, list the state before the event, and list the assertions after it. Then find the transition in <code>scheduler_state_machine.cc</code>. You have now connected an expectation to the branch that implements it. If you can build Chromium, change one input or add a log around that transition and rerun only the focused test. The question is whether the observed state change matches the model you drew.</p>`]
  ],
  checkout: [
    ['How a DEPS file becomes a checkout', `<p>The top-level <code>.gclient</code> file names the solution repository and options for this workspace. <code>src/DEPS</code> describes dependencies of that solution. <code>gclient sync</code> evaluates those declarations for the host and configured variables, fetches the selected Git and package revisions, and runs hooks that prepare generated or downloaded build inputs. <code>.gclient_entries</code> records paths managed by the previous sync, which helps gclient notice dependencies that have disappeared.</p><p>This is a graph operation, not a single <code>git pull</code>. A dependency can have its own DEPS file. The sync queue waits for a parent checkout before reading its DEPS, and serializes operations that need the same repository URL. The relevant implementation is in <code>depot_tools/gclient.py</code>. When a sync fails, first determine which node failed: parsing, condition evaluation, Git checkout, CIPD/GCS download, or a hook. Each suggests a different fix.</p><div class="code-path"><b>Follow the input</b><code>.gclient → src/DEPS → gclient evaluation → dependency queue → checkout or package → hook → generated GN input</code></div>`],
    ['Why DEPS is a restricted language', `<p>DEPS looks like Python, but <code>gclient_eval.py</code> parses a deliberately restricted syntax. It accepts selected assignments, literals, expressions, and helpers such as <code>Var</code>; conditions use their own constrained evaluator. Ordinary imports, function definitions, and arbitrary code are outside that grammar. The tests in <code>depot_tools/tests/gclient_eval_unittest.py</code> show both accepted and rejected examples.</p><p>The constraint solves a trust and reproducibility problem. A dependency manifest must express revisions and conditions without becoming an unconstrained program run during checkout. It also makes tooling able to inspect and rewrite pins. The tradeoff is that a complex condition may be awkward; the solution is usually to simplify the dependency declaration, not to hide logic in a script.</p>`],
    ['Pins, generated settings, and toolchain identity', `<p>A DEPS revision pin identifies code or a package, but the build also reads generated settings. <code>gclient_gn_args</code> selects variables written into <code>build/config/gclient_args.gni</code>. A GN file such as <code>build/toolchain/siso.gni</code> imports that file and makes a build decision. Local <code>out/.../args.gn</code> adds another input. Thus “same <code>src</code> commit” does not imply “same build.”</p><p>Compiler identity is similarly concrete. Chromium pins a Clang revision in <code>tools/clang/scripts/update.py</code>; downloaded archives and stamp checks tie the installed toolchain to that revision. If an ABI or optimization bug appears on only one machine, compare the actual compiler stamp and GN args before assuming the C++ source differs.</p><div class="study-example"><b>Diagnostic question</b><p>If CI succeeds and your build fails, write the exact <code>src</code> revision, DEPS pins, host OS, generated <code>gclient_args.gni</code>, <code>args.gn</code>, and Clang revision. Which is the first differing input?</p></div>`],
    ['From remote compilation to Siso', `<p>Build acceleration has changed across the material. Goma and later reclient/RBE provided remote execution paths; Siso is the build scheduler discussed by the current checkout and can use remote execution directly. Some older plumbing remains in the tree. Read <code>build/toolchain/siso.gni</code> and the GN/Ninja wrapper scripts for the studied revision instead of assuming every historical service is still on the critical path.</p><p>Remote execution helps only if an action has a complete, portable description of its inputs and outputs. Undeclared files, machine-local state, or nondeterministic tools cause cache misses or wrong results. The deeper lesson is that a fast distributed build depends on the same property as a reproducible build: an accurate model of every input.</p>`]
  ],
  owners: [
    ['The objects behind one tab', `<p><code>WebContents</code> is the browser-side container for tab-like content. Its frame tree represents a main frame and child frames. A browser-side <code>RenderFrameHost</code> corresponds to a frame hosted in a renderer and participates in document/navigation lifecycle. In the renderer, Blink has a <code>Page</code>, local and remote frame representations, DOM objects, and V8 contexts. Do not treat any one of these as “the page” without saying which state you mean.</p><p>Consider a page at <code>a.example</code> with an iframe at <code>b.example</code>. Site isolation can place the iframe in another renderer. The parent renderer still needs a representation of the remote child for geometry and communication, but it does not own that child’s DOM. The browser process retains the frame relationship and mediates cross-process operations. A renderer crash destroys state in that renderer, while browser-owned objects can remain long enough to show an error or navigate again.</p><div class="code-path"><b>Ownership sketch</b><code>Browser: WebContents → frame tree → RenderFrameHost(s)<br>Renderer A: Blink Page → local main frame + remote child proxy<br>Renderer B: Blink Page → local iframe document</code></div>`],
    ['Why site and frame are separate choices', `<p>Chromium does not choose a process by counting tabs. It reasons about sites and browsing relationships. <code>SiteInstance</code> and <code>BrowsingInstance</code> help express which documents may need synchronous script access and which process group can host them. Site isolation then imposes process separation for certain cross-site boundaries, subject to platform and policy details. Read <code>docs/process_model_and_site_isolation.md</code> for the policy and <code>docs/frame_trees.md</code> for the objects.</p><p>The design balances two constraints. Isolation gives the operating system a boundary between web origins and contains renderer failure. Too many processes increase memory and scheduling cost; too much separation can complicate synchronous relationships expected by the web platform. Process assignment is therefore a policy decision informed by the document and its relationships, not a property of the tab icon.</p>`],
    ['Where the module boundaries matter', `<p><code>content</code> supplies a reusable multiprocess browser core. <code>chrome</code> adds product behavior through embedder interfaces such as <code>ContentBrowserClient</code>. Blink implements web-platform behavior in the renderer. V8 executes JavaScript. <code>cc</code> manages compositor state and frame production; Viz aggregates compositor frames and draws to the display. These are code and responsibility boundaries, not a promise that each name maps to one operating-system process.</p><p>The Blink source tree has another dependency structure. <code>renderer/core</code> contains tightly coupled DOM, HTML, CSS, and rendering behavior; <code>modules</code> implements many more separable APIs; <code>platform</code> supplies lower-level facilities. <code>third_party/blink/renderer/README.md</code> describes intended direction, while per-directory <code>DEPS</code> rules, presubmit checks, and clang plugins enforce some boundaries. If you are deciding where a new feature belongs, identify its consumer and allowed dependency direction before choosing a directory.</p>`]
  ],
  navigation: [
    ['The old document remains real while a request is pending', `<p>Take a link click from document A to B. A renderer can request the navigation, but the browser process owns the transition to a new document. While the request is pending, A may remain visible and active. A redirect may change the destination; a policy check may reject it; a second navigation may replace it. Code that needs the currently committed document’s authority must use its committed origin. A pending or visible URL answers a different question.</p><p><code>docs/navigation_concepts.md</code> distinguishes last committed, pending, and visible URL. These are not merely display variants. If security code used the pending destination as the current origin, a page could acquire authority before any document at that origin committed. Draw the browser and renderer timelines separately whenever a navigation bug involves a URL that “already changed.”</p>`],
    ['A cross-document call path', `<p>At the studied revision, a renderer-started navigation can begin in Blink’s <code>FrameLoader::StartNavigation</code> and reach its embedder through <code>WebLocalFrameClient::BeginNavigation</code>. The content renderer sends a <code>FrameHost::BeginNavigation</code> request to the browser, which creates or updates a <code>NavigationRequest</code>. After loading and checks, the browser asks a renderer to commit through <code>NavigationClient::CommitNavigation</code>; content’s <code>RenderFrameImpl::CommitNavigation</code> reaches Blink’s <code>WebNavigationControl::CommitNavigation</code> and <code>DocumentLoader::CommitNavigation</code>. The commit constructs the new document environment.</p><p>This path is a guide to source archaeology, not a claim that every navigation takes identical branches. Same-document navigation, error pages, redirects, speculative hosts, and browser-initiated requests differ. The named Blink implementation files are absent from the sparse checkout; use the pinned upstream links in the source list and verify the branch for the scenario you study.</p><div class="code-path"><b>Question at each boundary</b><code>Who initiated it? → Which browser request is active? → Which frame host receives commit? → When does Blink replace the document?</code></div>`],
    ['A document also needs a loader', `<p>Commit is not the end of loading. Blink creates a document and parser, then discovers subresources. A document’s <code>ResourceFetcher</code> and <code>ResourceLoader</code> coordinate requests through the loader factory provided at commit. This separation matters when a navigation appears successful but images, scripts, or stylesheets fail. Check whether the failure is before commit, during document parsing, or in a later resource request.</p><p>For a debugging trace, record request ID, frame ID, process ID, and document identity. A late callback from document A can arrive after B has committed. The callback must be tied to the right loader and lifetime; the fact that its URL looks relevant is insufficient.</p>`]
  ],
  time: [
    ['A task is scheduled work, not a suspended function', `<p>When code calls <code>PostTask</code>, it gives a task runner work to execute later. The posting stack returns; the callback gets a new stack when its sequence chooses it. The runner also defines an ordering domain. Blink asks a <code>FrameScheduler</code> for runners by <code>TaskType</code>, allowing input, loading, timers, and other work to receive different queue policy. Once a main-thread task starts, it generally runs until it yields or returns; a high-priority input task cannot preempt a long JavaScript callback halfway through.</p><p>That gives a concrete diagnosis for sluggish input: the scheduler may prioritize input correctly, yet a long task already running can still delay it. Search for the posting site, queue selection, and callback body. A stack trace from the callback alone cannot show why it was delayed.</p>`],
    ['Main thread, compositor thread, and workers', `<p>The renderer main thread runs much of Blink’s DOM, style, layout, and ordinary page JavaScript. A compositor thread can handle eligible input and frame work without waiting for every main-thread operation. Workers have their own backing threads and V8 isolates, so “renderer thread” is too vague to predict where a callback runs. Blink’s <code>Page</code> owns frame structure, and local roots connect to compositor state through a widget and <code>LayerTreeHost</code>.</p><p>For input, one documented route is browser <code>RenderWidgetHostImpl</code> → <code>InputRouterImpl</code> → renderer <code>WidgetInputHandlerImpl</code> on the compositor side → <code>cc::InputHandlerProxy</code>. If compositor handling cannot complete the event, it can go to Blink on the main thread, where target dispatch occurs. An acknowledgement travels back. Ask whether the event needed main-thread script before claiming it was “handled by the compositor.”</p>`],
    ['Tasks, microtasks, and frame updates', `<p>JavaScript promises use microtasks. A microtask checkpoint is associated with event-loop and script-scope rules; it is not a timer and does not require a new browser process message. Blink’s agent has an event loop and microtask queue, and V8 performs checkpoints at defined boundaries. A chain that continually adds microtasks can delay ordinary tasks and rendering.</p><p>Frame production has its own schedule. The compositor requests a main-frame update; Blink updates lifecycle stages such as style, layout, pre-paint, and paint when needed. The statement “paint runs after every task” is false. A task may leave no visual change, and frame work may wait for a rendering opportunity. To investigate a delayed pixel, record the end of the script task, the microtask checkpoint, BeginMainFrame, lifecycle updates, commit, raster, and display. The first missing or late event narrows the fault.</p>`],
    ['How to prove a thread claim', `<p>Source names alone can mislead: an interface may be declared in one component and invoked on another sequence. At a crossing, find the task runner or Mojo endpoint; then inspect sequence checks, binding setup, or tests. Instrument with process ID, thread ID, frame ID, and a timestamp at sender and receiver. If events seem out of order, remember that Mojo guarantees FIFO within a message pipe, not an ordering across independent pipes.</p><p>For an experiment, add trace events around one input callback and one frame update. Move the pointer while running a long script task. If compositor-thread scrolling continues but a script-driven visual update stalls, you have observed two scheduling paths with different dependence on the main thread. Eligibility varies, so record the actual event path rather than generalizing from one page.</p>`]
  ],
  pixels: [
    ['Style and layout produce different answers', `<p>Computed style answers which CSS values apply after cascade, inheritance, and resolution. Layout answers geometry under constraints such as available inline size. LayoutNG represents a layout computation with inputs such as a node, computed style, constraint space, and sometimes a break token; the result contains immutable physical fragments. If constraints and relevant inputs have not changed, cached results can avoid repeating work.</p><p>Imagine changing a paragraph’s text color. Geometry is unchanged, but paint output changes. Now change its width: line breaks and fragment positions may change, so layout is needed. If a visual bug appears, inspect the earliest stage whose output differs from expectation. This is more precise than saying “Blink failed to render.”</p>`],
    ['Pre-paint and paint record a scene', `<p>Before paint, Blink tracks invalidation and computes property state for transforms, clips, effects, and scrolling. Paint records drawing commands grouped into paint chunks with associated property state. The result is a paint artifact: a description of how to draw, not the final pixels. <code>PaintArtifactCompositor</code> turns suitable chunks and properties into compositor layers.</p><p>These property trees matter because a scrolling or transform change may be applied without redoing all layout and paint. But eligibility is conditional: clipping, effects, dependencies on main-thread state, and other content can force more work. Inspect the paint artifact and compositor layer state for the particular page before promising a “compositor-only” change.</p><div class="code-path"><b>Data changes shape</b><code>DOM + CSS → ComputedStyle → layout fragments → paint chunks + property trees → cc layers</code></div>`],
    ['Why the compositor keeps more than one tree', `<p><code>cc</code> receives a commit from the main-thread host and prepares a pending tree while an active tree can still be displayed. Raster work turns needed tiles into pixel resources. When ready, the pending state activates; a compositor frame is submitted. Viz aggregates frames from surfaces, including separate renderers, and draws the final display output. Read <code>cc/README.md</code> and <code>docs/how_cc_works.md</code> beside <code>cc/trees/layer_tree_host.cc</code>.</p><p>The extra tree and frame stages address a timing problem: a page must remain drawable while new state is prepared. They cost memory and synchronization complexity. When an update appears one frame late, ask whether Blink produced new paint, whether cc committed and activated it, whether raster completed, and whether Viz displayed the submitted frame.</p>`],
    ['A useful performance experiment', `<p>Create two versions of a small page: one animates <code>transform</code>, the other animates <code>width</code>. Record a Performance trace for each. Hypothesis: width changes require repeated layout; a simple transform may update through compositor state. Inspect actual Layout, Paint, Commit, and raster events rather than relying on property names alone. Add a long main-thread task and compare frame delivery.</p><p>If both versions show layout, investigate whether script reads geometry or changes another property. If transform still stalls, check whether the layer is eligible for compositor animation and whether the main thread is required for the update. The point of the experiment is to identify the first stage that differs, then explain the condition that caused it.</p>`]
  ],
  bindings: [
    ['Web IDL is a program input', `<p>For a DOM method, the <code>.idl</code> file is the web-facing contract: name, types, overloads, attributes, inheritance, exposure, and sometimes runtime conditions. Blink collects IDL files, builds a database that resolves partial interfaces and mixins, then generates bindings. The implementation method may live in <code>renderer/core</code> or <code>renderer/modules</code>. Generated C++ performs argument conversion, exception handling, and wrapper interaction before it calls that method.</p><p>This is why a search for the method name in handwritten C++ can skip the actual JavaScript entry point. Start at IDL; inspect generated output from a build when behavior depends on conversion or exposure; then read the implementation and a web test. Generated files may not exist in the sparse source tree before a build.</p><div class="code-path"><b>One API path</b><code>interface.idl → IDL database → generated V8 binding → Blink C++ method → optional Mojo service</code></div>`],
    ['Where the JavaScript object lives', `<p>V8 runs script in a <code>Context</code>. Blink’s <code>ScriptState</code> represents that context, while a frame can have different execution worlds, including isolated worlds used by extensions. A JavaScript wrapper refers to a Blink object through the V8/Blink bridge. Exposure must be evaluated for the current world and global object. Sharing a context across security boundaries would make properties and authority leak, so world identity is part of the model.</p><p>The outer <code>WindowProxy</code> can persist across a normal navigation while the inner window, document, and global environment change. That explains a confusing observation: a reference to <code>window</code> may still be usable after navigation, while its document properties now refer to a different document. Inspect the proxy and inner-global distinction before reasoning about cached window references.</p>`],
    ['Generated tables also shape HTML and CSS', `<p>Blink uses generated data outside Web IDL. HTML tag names come from <code>core/html/html_tag_names.json5</code>; generator output helps map a tokenized name to an element class. The parser path goes through <code>HTMLTokenizer</code>, <code>AtomicHTMLToken</code>, tree-builder insertion modes, and <code>HTMLConstructionSite::CreateElement</code>. The mapping is not one class per tag; many tags share implementation. A custom element constructor can run during creation, so parser code must tolerate reentrant script.</p><p>CSS properties come from <code>core/css/css_properties.json5</code>. Generated identifiers and lookup data turn textual property names into compact IDs and metadata. This saves repeated string work and centralizes feature data, but it creates a debugging step: when you cannot find a handwritten declaration, look for the generator input and generated output. Counts in the research are revision-specific.</p>`],
    ['A complete feature investigation', `<p>Choose a web API and answer five questions. First, where is its IDL declaration and which globals expose it? Second, which runtime feature flag controls it, if any? Third, which C++ object owns its state? Fourth, does the call stay in the renderer or cross to a browser-side Mojo endpoint? Fifth, which web test asserts the behavior?</p><p>Feature definitions live in <code>platform/runtime_enabled_features.json5</code>; generated code installs or hides bindings under those conditions. A flag’s declared status does not prove a particular user has it enabled, because field configuration may alter runtime state. If your experiment differs from the source default, inspect command-line flags, field trials, and test setup. Keep the implementation fact separate from the observed rollout state.</p>`]
  ],
  lifetime: [
    ['Tracing answers reachability', `<p>Oilpan uses cppgc to trace managed Blink objects. A <code>Member&lt;T&gt;</code> field in a managed object is a strong edge the collector visits when its <code>Trace</code> method reports it. <code>WeakMember&lt;T&gt;</code> does not keep its target alive in the same way. A <code>Persistent&lt;T&gt;</code> can root a managed object from outside the heap. Blink’s <code>platform/heap/member.h</code> aliases these cppgc types; <code>v8/include/cppgc/member.h</code> and <code>persistent.h</code> describe their semantics.</p><p>Draw a graph, not a list of pointers. If JavaScript holds a wrapper, the V8/Blink bridge must account for the cross-heap relationship. If a C++ object owns a <code>Member</code>, its tracing method must expose the edge. If the only remaining edge is weak, the target may be collected. The exact allocation and tracing rules of a class matter more than the name of the subsystem containing it.</p>`],
    ['Reachable does not mean valid for the current document', `<p>A posted callback may keep an object in memory after its document is detached. That can be memory-safe and still wrong: a result from the old document might update the new page, send an obsolete IPC, or resolve a promise that no longer belongs to the active operation. Investigate two separate questions: can the callback dereference this object safely, and should the operation still happen?</p><p>Look for cancellation on navigation, document lifecycle checks, weak callbacks, and sequence checks. Also look for the owner of the asynchronous operation: <code>Document</code>, <code>LocalFrame</code>, a loader, or a service object. If you cannot find an explicit cancellation path, search tests that navigate away while work is pending. The test often reveals the intended failure behavior more clearly than the happy-path method.</p>`],
    ['Why multiple memory systems coexist', `<p>Not all Blink memory is Oilpan-managed. <code>StringImpl</code>, for example, uses reference counting, and other buffers use PartitionAlloc. V8 has its own JavaScript heap. This is a deliberate mix of lifetimes and costs, not a single universal pointer rule. Old documents in the archive may describe allocator choices that changed; inspect the current type declaration and allocation operator at the pinned revision.</p><p>For a source review, label each edge as raw pointer, reference-counted handle, cppgc member, weak member, persistent root, or V8 handle. Then add the thread or sequence on which it is accessed. A collector cannot make a cross-thread access race-free, and a reference count cannot decide whether a page navigation made an operation obsolete.</p>`]
  ],
  graphs: [
    ['Why a list of instructions can overconstrain optimization', `<p>In a basic block, a list says that operation A appears before B even if both are pure and independent. A dependency graph instead records only the order required to compute values and preserve effects. For <code>x = a + b; y = c * d</code>, the two calculations have no dependency on each other. A scheduler may place them in whichever legal order suits the target machine.</p><p>That freedom disappears where semantics require it. If <code>o.x = 1</code> is followed by <code>o.x = 2</code>, the writes need an effect order. A load must see the correct preceding store. The graph’s value, control, and effect inputs form a partial order; the scheduler later chooses one legal total order.</p>`],
    ['Branches need matching value and control', `<p>Suppose a branch assigns <code>x = 1</code> on one path and <code>x = 2</code> on the other. At the join, a PHI node chooses the value associated with the incoming control edge of a REGION. The pairing is essential. If an optimizer moves or rewrites a branch, it must preserve the association between incoming path and incoming value. A PHI is not a runtime “choose any value” instruction.</p><div class="code-path"><b>At a join</b><code>if true  → x₁ = 1 ┐<br>                 REGION → PHI(x₁, x₂) → use x<br>if false → x₂ = 2 ┘</code></div><p>Draw the control edge alongside the value edge for any optimization that touches a branch. It makes illegal transformations obvious before you inspect machine instructions.</p>`],
    ['The cost of a flexible graph', `<p>The original Sea of Nodes paper explains how a graph can make placement and movement more explicit. It does not, by itself, settle alias analysis, final instruction scheduling, or modern cache costs. V8’s later compiler work moved toward a more explicit control-flow representation in Turboshaft for reasons discussed in the supplied writing, including readability and compilation cost. That change does not make graph IR “wrong”; it shows that an abstraction can become expensive when common operations need extra edges and the optimizer spends time reconstructing order.</p><p>For Chromium study, separate the paper’s proposal, TurboFan’s historical implementation, and the current V8 checkout. The standalone V8 tree supplied here is sparse and at a different revision from Chromium’s pin, so it cannot verify every compiler implementation claim locally. Use the pinned V8 source before drawing conclusions about current code.</p>`]
  ],
  factory: [
    ['Configuration is compiled before a build runs', `<p>Chromium’s <code>infra/config</code> Starlark files describe builders and LUCI services. <code>lucicfg</code> generates service configuration from them. A builder is the named recipe and parameter configuration; a build is one Buildbucket execution of it; Swarming tasks are units of work scheduled onto machines. The recipe code for some builds lives under <code>chromium/tools/build</code>, outside this sparse checkout. Start with <code>infra/config/main.star</code> and <code>docs/infra/glossary.md</code>, then inspect the generated Buildbucket config for the exact builder.</p><p>If a CI result surprises you, ask which layer you are inspecting. A builder definition tells you intended configuration, a build record tells you what ran, a task record tells you where and with what outcome, and ResultDB holds test results. No one layer alone explains every failure.</p>`],
    ['Follow a change through the commit queue', `<p>A developer uploads a patch set to Gerrit. Presubmit checks can run locally and on upload. Review and OWNERS requirements determine whether the change is eligible. The commit queue selects configured tryjobs, usually testing the patch against a particular base. A successful set of required checks allows submission; post-submit builders then test the resulting main-branch state.</p><p>The CQ uses multiple services. CV Run states include pending, running, waiting for submission, submitting, and terminal outcomes. Gerrit performs the actual merge/rebase operation; other infrastructure assigns commit-position metadata. Treat “the CQ landed it” as shorthand for a coordinated state machine. If a tryjob passed but the landed revision fails, compare patch set, base, DEPS materialization, builder selection, and post-submit environment.</p>`],
    ['A red test is evidence, not a verdict', `<p>A failing test can indicate a regression in the patch, an existing failure on the base, flaky behavior, or infrastructure trouble. Chromium’s CI can retry tests and compare runs with and without the patch; LUCI Analysis and ResultDB help classify and record results. The exact retry and attribution rules vary by builder, so inspect the build and test records for the case at hand.</p><p>Use a small causal table: did the same test fail without the patch, did it pass on retry, was the failure on a different shard, and did the test binary or data change? That is distributed-systems debugging: you need the code identity and the execution identity. A green summary can also hide tests that were never selected, so read the builder’s test list before treating green as universal coverage.</p>`],
    ['Landing is separate from shipping', `<p>Main-branch submission does not immediately put code in a stable release. Release branches, merges, and rollout decisions form another lifecycle. The checked-in <code>docs/process/release_cycle.md</code> gives the process for the studied revision; scheduling and branch policy can change, so cite that revision when explaining a milestone. Public sources in this archive do not expose every internal release or performance service.</p><p>When investigating “which users have this fix?”, trace the commit from main to branch, the branch build, and the feature’s runtime enablement. A feature flag may remain off even when code is shipped. This is the same evidence habit used throughout the course: repository presence, build inclusion, and runtime exposure are different claims.</p>`]
  ],
  independent: [
    ['Write a source-backed engineering note', `<p>Choose one observable behavior and produce a one-page note with a narrow title, such as “What happens to a cross-site iframe when its renderer crashes?” Start with a diagram of the process and object owners. Then give a numbered runtime sequence with exact symbols and source paths. Include one test that checks a relevant invariant and one observation from a run, trace, or focused instrumentation. End with two plausible failure modes and the next experiment that would distinguish them.</p><p>Every sentence should be classifiable as documented design, implementation fact, runtime observation, or inference. Label uncertain steps. This is not an academic formality: it lets a reviewer challenge the precise part of the model that might be wrong.</p>`],
    ['Make the next investigation less guided', `<p>On the first investigation, use the chapter’s source list. On the second, begin from a failing test or a user-visible symptom and find the relevant subsystem yourself. On the third, choose a neighboring feature that crosses a boundary you have not studied: perhaps a Blink API that sends Mojo to the browser, or a compositor update that arrives from a cross-process iframe. The aim is to transfer the method, not to memorize the chapter diagrams.</p><p>A useful stopping point is a question you can hand to another engineer: the current hypothesis, the file or trace event that would decide it, and the expected outcomes. If the sparse checkout lacks that file, give its pinned upstream path. If the CI record is private or gone, state which claim remains unverified. Independent source work includes knowing when evidence is missing.</p>`]
  ]
};
