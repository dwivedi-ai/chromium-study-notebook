// Further chapter sections covering the full Blink research narrative.
export const blinkComplete = {
  owners: [
    ['Why Blink forked, and why that history matters', `<p>Blink began as a WebKit fork in 2013. The documented engineering reason was the cost of maintaining Chromium’s multiprocess architecture inside a codebase also serving other architectures. The first announced change was subtraction: removing obsolete build systems and millions of lines that Chromium did not need. Chrome 28 carried the transition. This history explains a current boundary: Blink implements web-platform behavior in a sandboxed renderer, while content supplies the browser process and platform integration.</p><p>Two later changes were also structural. The old RenderObject, LayoutObject, and later LayoutNG/RenderingNG work changed the representation of layout and painting while the browser kept shipping. Oilpan’s collector moved into V8’s cppgc rather than staying a separate Blink collector. A reader of old design documents can therefore encounter valid historical terminology that no longer names the current implementation. Before following an old call path, check its commit and the current header. The fork and rewrites are useful as examples of changing a system’s foundations without replacing its user-facing contract all at once.</p>`],
    ['Blink’s directory boundaries are enforced', `<p>At the pinned revision, most of <code>third_party/blink</code> consists of web tests; the runtime implementation is chiefly <code>renderer/</code>. Within that, <code>core</code> holds interdependent DOM, HTML, CSS, layout, and painting code; <code>modules</code> groups more separable web APIs; <code>platform</code> holds lower-level utilities; <code>bindings</code> generates the V8-facing surface. <code>public</code> is the interface content uses to embed Blink. The architecture is a dependency direction, not merely a folder naming scheme.</p><p><code>renderer/README.md</code> describes the direction from public/web through modules and core toward platform and the smaller public layers. Per-directory <code>DEPS</code> files restrict includes. Presubmit checks reject certain unwanted APIs, while compiler plugins check Blink data-member and garbage-collection rules. These gates catch different errors at different times. If a proposed feature in <code>platform</code> needs to include <code>core/dom/document.h</code>, that is a signal that the abstraction is probably at the wrong layer. The code should be reorganized around the allowed direction instead of bypassing the check.</p><p>Counts in the source research are a snapshot: 213,000 tracked files under Blink, 189,644 under web tests, and 18,997 under renderer at <code>src@f288fed6</code>. The lesson is that the most visible directory size is mostly tests; use the dependency graph, not file count, to find the implementation boundary.</p>`],
    ['Process placement has more than one identity', `<p><code>SiteInfo</code> describes the security principal used for process assignment, commonly a site rather than a full origin. <code>SiteInstance</code> represents an instance of that principal, and <code>BrowsingInstance</code> groups related browsing contexts. Documents that require synchronous access within a browsing context group constrain placement. The browser makes this decision; Blink runs inside whichever renderer it receives. The exact policy differs by platform and may change behind features, so a page URL alone cannot prove a process ID.</p><p>The supplied Blink research also found an experimental Multiple Blink Isolates design. Its modes distinguish the legacy process-wide IPC channel from more granular agent-scheduling-group channels. A source enum’s existence is not proof that the feature is generally enabled; at the studied revision the code warns that the feature is not fully implemented. This is a valuable source-reading trap: an architectural plan can be present in code without being the runtime default. Check buildflags, feature defaults, and actual process traces before explaining a customer-visible behavior.</p>`],
    ['The public Blink API is an embedder contract', `<p><code>third_party/blink/public/README.md</code> divides the interface into <code>public/common</code> for shared definitions, <code>public/platform</code> for services Blink asks the embedder to provide, <code>public/web</code> for operations the embedder calls into Blink, and public Mojo definitions. Content is the primary consumer. This interface keeps content from depending on Blink internals, while allowing both sides to change together in one source tree.</p><p>At renderer startup, content’s <code>RenderThreadImpl::InitializeWebKit</code> constructs its platform implementation, calls <code>blink::Initialize</code>, and normally initializes a compositor thread. Later, creating a page involves <code>AgentSchedulingGroup::CreateWebView</code> and <code>WebView::Create</code>. A frame uses <code>RenderFrameImpl</code> as a <code>WebLocalFrameClient</code>. Those are useful entry points because they make the ownership inversion visible: content provides the environment and invokes Blink; Blink calls back through interfaces when it needs navigation or platform work.</p>`]
  ],
  navigation: [
    ['Follow one input event across the boundary', `<p>An input event may begin in the browser’s <code>RenderWidgetHostImpl</code>, pass through <code>InputRouterImpl</code>, and reach the renderer’s <code>WidgetInputHandlerImpl</code>. The compositor-side <code>cc::InputHandlerProxy</code> may handle eligible scrolling without waiting for Blink main-thread script. Otherwise the event reaches Blink target dispatch on the main thread. An acknowledgement returns to the browser. This path explains why two input events can have very different latency even in the same tab.</p><p>When tracing an input bug, record event ID, process, thread, target frame, and acknowledgement. If the renderer main thread is blocked, a compositor-handled scroll may continue while a click listener does not. If the event targets a remote iframe, a different renderer may own the listener. The browser’s input routing, Blink dispatch, and page JavaScript are separate places to instrument.</p>`],
    ['Navigation and loading cross different interfaces', `<p>For a renderer-initiated cross-document navigation, Blink’s <code>FrameLoader::StartNavigation</code> calls its <code>WebLocalFrameClient</code>; content sends a <code>FrameHost::BeginNavigation</code> message; the browser tracks a <code>NavigationRequest</code>. After response and policy checks, the browser asks a renderer to commit. Content’s <code>RenderFrameImpl::CommitNavigation</code> reaches Blink’s <code>DocumentLoader::CommitNavigation</code>, which creates the new document environment. Each arrow is a possible cancellation or lifetime boundary.</p><p>After commit, subresources take a related but distinct path. The document has a <code>ResourceFetcher</code>; resource loaders use the URL loader factory supplied for that document. If a page displays its main HTML but misses CSS, do not restart your investigation at process selection. Check parser discovery, resource request creation, factory binding, response delivery, and error handling. The source research’s call path is at the pinned revision and is not a universal path for same-document navigation or browser-initiated special URLs.</p>`]
  ],
  time: [
    ['Why frame-specific task queues exist', `<p>The HTML specification speaks in terms of event-loop task sources. Blink implements ordinary task queues at frame granularity because the scheduler needs to pause, throttle, or prioritize individual frames. <code>FrameScheduler::GetTaskRunner(TaskType)</code> selects a queue according to the kind of work. Queue traits determine whether a queue can be throttled or frozen; priority policy responds to input, loading, visibility, and other state. The event loop object mainly coordinates microtasks for an agent.</p><p>Priority does not preempt a running callback. A long task still blocks the renderer main thread until it returns. The scheduler can prefer pending input over lower-priority work after that point. A hidden frame may be throttled or frozen, but exact delays are flag-dependent and have changed; do not copy an old five-minute number into a current explanation. To test queue behavior, trace posting and execution timestamps for two task types while toggling visibility. The difference between “posted” and “ran” is the observable scheduling decision.</p>`],
    ['A frame update is a scheduled transaction', `<p>The compositor requests a main-frame update by posting work to Blink’s main thread. Blink then advances the document lifecycle through any needed style, layout, pre-paint, and paint work before committing compositor state. It is better to think of this as a transaction over several cached representations than as “draw DOM.” The compositor can keep an active tree visible while preparing newer state. The resulting frame may be rasterized and sent to Viz for aggregation with other surfaces.</p><p>A useful trace question is “where did the new state first appear?” If script mutated style but no BeginMainFrame followed, investigate scheduling. If style/layout ran but paint did not change, inspect invalidation. If paint changed but the screen did not, inspect cc commit, activation, raster, and Viz submission. This turns a vague visual symptom into a search for the first missing state transition.</p>`]
  ],
  parsing: [
    ['Generated HTML and CSS tables at this revision', `<p>The pinned Blink research counted 153 HTML tag entries mapping to 90 element classes. Several tags intentionally share a class; heading tags share <code>HTMLHeadingElement</code>, and ordinary tags can use <code>HTMLElement</code>. The generator emits qualified-name definitions, a tag enum, a lookup implementation, an element factory, and type helpers. A recognized token caches its tag ID before the tree builder applies insertion modes. A runtime-gated tag cannot be decided entirely by the tokenizer because exposure can depend on a document or origin.</p><p>The CSS input contained 828 property entries: longhands, shorthands, and aliases. Generated identifiers and a name lookup encode the common path efficiently. A CSS property’s textual spelling, parser ID, computed-style storage, and layout effect are different things. For <code>width</code>, follow the JSON5 row into generated property metadata, then into parsing, cascade, and computed style before looking at layout. Counts are for <code>src@f288fed6</code>; the generator inputs and output are the durable way to inspect a newer tree.</p>`]
  ],
  bindings: [
    ['The scale and phases of Web IDL generation', `<p>The studied Blink build listed 2,242 IDL inputs, split between core and modules, and generated thousands of C++ binding paths. The compiler first collects parsed IDL syntax trees. A database-building pass merges partial interfaces and mixins, resolves inheritance and overloads, and calculates exposure. Per-kind generators then produce V8-facing C++ for interfaces, callbacks, dictionaries, unions, and other categories. Parallel GN actions make that generation practical in a large build.</p><p>The distinction between source input and generated output matters for debugging. If a method has several overloads, the handwritten C++ implementation may never run for an invalid JavaScript call: generated argument checks can reject it first. If an interface is exposed only to <code>Window</code> and not a worker, that decision can be compiled into installer logic. Read the IDL declaration, the merged database or generated binding, the implementation, and the web test in that order.</p>`],
    ['Contexts, worlds, and snapshots', `<p><code>ScriptState</code> corresponds to a V8 <code>Context</code>. A frame can host more than one execution world, including isolated worlds used by extensions. A world has its own global object and binding exposure; sharing all state across worlds would break isolation. Blink can create a context directly or from a startup snapshot that contains prebuilt template state. The snapshot is a build artifact, so again the runtime behavior depends on generated inputs as well as handwritten code.</p><p>The <code>WindowProxy</code> is the stable outer reference visible to script. A normal cross-document navigation replaces the inner window, document, and context while the outer proxy can remain. This explains a practical debugging surprise: a stored <code>window</code> reference persists, but properties read through it may now refer to another document. To verify, hold a window reference across navigation and inspect its document before and after; then trace the proxy’s context swap in the pinned source.</p>`]
  ],
  pixels: [
    ['From immutable stage outputs to caches', `<p>Style produces a <code>ComputedStyle</code>. LayoutNG consumes a node, style, and <code>ConstraintSpace</code>, and returns a <code>LayoutResult</code> containing immutable physical fragments. Pre-paint computes invalidation and transform, clip, effect, and scroll property trees. Paint produces a <code>PaintArtifact</code> with chunks and property state. These outputs are not incidental intermediate objects: their immutability makes it possible to compare inputs and reuse earlier results safely.</p><p>For example, if a block receives the same constraints and relevant style, a cached layout result may remain valid. If a color changes, geometry can remain cached while paint changes. If a transform changes, compositor property state may update without rebuilding all fragments. Each optimization has conditions; the safe question is which input changed and which stage’s cached output became invalid. <code>DocumentLifecycle</code> asserts the legal stage order. Older documentation may name states removed from the studied enum, so use the header for exact states.</p>`],
    ['The handoff from paint to Viz', `<p>Blink’s compositing algorithm maps paint chunks and property trees into cc layers. A commit moves main-thread state toward the compositor thread, which can hold active and pending trees. Raster produces tile resources; a compositor frame packages the surface; Viz combines surfaces, including those from different renderer processes, and draws the display output. This is why a cross-process iframe can appear as one visual page even though its DOM lives elsewhere.</p><p>Every boundary has its own failure mode. Paint can be correct while layerization is wrong; the pending tree may fail to activate; raster may miss resources; Viz may receive an old surface. A performance trace and compositor debugging view can distinguish them. The user-visible frame is the final product of several subsystems, so a screenshot alone cannot identify which stage produced a defect.</p>`]
  ],
  lifetime: [
    ['Oilpan, cppgc, and the remaining allocators', `<p>Blink once maintained its own tracing collector, Oilpan. The studied code uses V8’s cppgc for the managed heap, while Blink’s <code>platform/heap</code> directory supplies aliases, thread state, collection adapters, and tracing glue. <code>Member</code>, <code>WeakMember</code>, and <code>Persistent</code> are the types a class author commonly encounters. A <code>Trace</code> method exposes strong edges to the collector; a weak edge does not keep an object alive. The build’s garbage-collection plugin checks some pointer and tracing mistakes.</p><p>There is no single “Blink allocator.” <code>StringImpl</code> is reference-counted, buffers can use PartitionAlloc, JavaScript objects live in V8, and DOM wrappers bridge V8 and cppgc. An old document that says all layout objects use PartitionAlloc is historical at this revision. When tracing a leak or use-after-free, identify each allocation domain and each edge that crosses it. Then inspect teardown and task cancellation separately: the collector protects reachability, not the semantic validity of work after navigation.</p>`]
  ],
  features: [
    ['A feature row is only the first gate', `<p>The pinned <code>runtime_enabled_features.json5</code> contained 1,227 entries. The research parsed 564 plain <code>stable</code>, 301 <code>experimental</code>, 90 <code>test</code>, 90 platform-specific status dictionaries, and 182 without a status. It also found 95 origin-trial names, 79 dependency relationships, and 26 implied-by relationships. These are counts of structured rows at one revision; a text grep can miscount formatting variations.</p><p>The generator compiles those rows into compact feature-state accessors. Some checks use a global flag; an origin-trial feature needs a <code>FeatureContext</code> because one origin can be eligible while another is not. Command-line switches and field configuration can override defaults. Generated bindings then decide whether a JavaScript API is installed. A web test often enables a feature explicitly, so a passing test does not establish that the feature shipped to stable users.</p><p>The source material contains conflicting undated guidance on how long an origin trial may run. That remains an open policy question, not a number the course should guess.</p>`],
    ['Test expectations are part of the feature story', `<p>Blink’s <code>web_tests</code> tree contains local tests, platform-specific baselines, virtual suites, and a mirrored web-platform-tests corpus. At the pinned revision, most tracked files under Blink were tests. <code>TestExpectations</code> records known failures and skips; a test can appear green because it is expected to fail or excluded on a platform. Read the test body, the runner flags, virtual-suite configuration, and expectation line before interpreting a dashboard result.</p><p>For an API rollout, compare tests under test-only, experimental, and stable feature states. A virtual suite can run the same test set with a feature enabled without changing the global default. A WPT test checks standardized behavior shared across engines, but a local test may cover Chromium-specific integration. The combination tells you both what the web contract requires and what this implementation currently promises.</p>`]
  ]
};
