export const ciComplete = {
  factory: [
    ['Four pipelines share LUCI, but have different jobs', `<p>Pre-submit checks a proposed Gerrit change before it lands. Post-submit rebuilds and retests main-branch commits. Performance testing measures a narrower set of revisions on controlled machines. Release automation creates and maintains branches and build artifacts. They share LUCI services such as Buildbucket, Swarming, ResultDB, and the web UI, but their triggers, owners, and acceptable latency differ. A passing tryjob answers a different question from a performance regression or stable rollout check.</p><p>The vocabulary matters. A <em>builder</em> is a named configuration, a <em>build</em> is an execution of it, a Swarming <em>task</em> is scheduled work on a bot, and a <em>recipe</em> defines much of the execution flow. If someone says “the Linux bot failed,” ask for the builder name, build ID, failing step, task ID, and source revision. Without those identities, the report is too ambiguous to debug.</p>`],
    ['How configuration reaches the fleet', `<p><code>infra/config</code> is source-controlled Starlark processed by <code>lucicfg</code>. It emits raw configuration for Buildbucket, Scheduler, CQ, Swarming, notification, and test targets. Presubmit checks keep generated output synchronized with authored input. The LUCI Config service imports a validated revision from Git; a configuration change therefore moves through code review and source history rather than a direct dashboard edit.</p><p>That makes fleet changes reviewable and branchable. It also means urgent changes wait for validation and propagation. At the pinned revision the research counted 125 Starlark files and 3,555 generated files under this configuration, but the exact count is less important than the dependency: authored config → generated config → import → service behavior. When a builder does not reflect a merged change, inspect the generated diff, imported config revision, and service’s active version before changing the recipe.</p>`],
    ['The Linux Builder as a configuration example', `<p>The studied <code>ci/Linux Builder</code> declaration names a gclient configuration, Chromium build mode, GN-argument mixins, compile targets, and contact team. Group and bucket defaults supply pool, OS, CPU, trigger, and timeout. Generated Buildbucket config turns that inheritance into a runnable builder. The runtime bootstrapper obtains a recipe bundle and reads build properties from the revision being tested.</p><p>A single builder name thus spans several source locations. If the bot checks out an unexpected dependency, inspect gclient configuration and build properties. If its GN args differ, follow mixins through generated <code>gn-args.json</code> and MB. If the wrong machine runs it, inspect inherited pool and dimension defaults. A builder definition is a declarative request; the build record is evidence of what actually executed.</p>`],
    ['Two generations of test configuration coexist', `<p>The older <code>testing/buildbot</code> system expands waterfall, suite, and exception files into bot JSON. Its exceptions can remove tests or modify how they run, but cannot add a test to a suite by themselves; that keeps a named suite from silently drifting. Newer Starlark targets under <code>infra/config/targets</code> generate shared test metadata and builder-specific target bundles. The transition leaves both kinds of file relevant at the pinned revision.</p><p>Between a builder and GN sits MB, <code>tools/mb/mb.py</code>, which resolves builder identity to GN arguments. Runtime shard count may be adjusted beyond a static declaration, and individual shards become Swarming tasks. To answer “did this change run test T?”, follow builder target selection, generated test specification, runtime shard plan, task record, and ResultDB result. Finding T in a source suite is not enough.</p>`],
    ['From upload to a selected tryjob', `<p><code>git cl upload</code> runs local upload presubmit, then pushes a patch set to Gerrit. Cloud presubmit and review requirements are separate gates. A CQ+1 dry run asks for configured tryjobs without submitting the change; CQ+2 requests full submission when other requirements are met. A direct <code>git cl try</code> with an explicit builder can schedule a Buildbucket run outside the usual CQ selection path.</p><p>Which tryjobs are selected comes from generated CQ configuration and the files affected by the change. A second request may reuse recent equivalent results instead of running everything again. That saves compute but makes the tested identity worth checking: patch set, base revision, builder configuration, and any reuse record. A changed patch set can cancel a CQ run; a rework can invalidate previously running builds. Read the actual CV Run and Buildbucket records before treating “CQ+1 passed” as a timeless property of the CL.</p>`],
    ['The retry ladder is causal attribution', `<p>The pinned CQ document describes a sequence: compile affected tests, run shards with the patch, retry failing shards under the same configuration, then run remaining failures without the patch. A result that disappears on an identical retry is evidence of flakiness. A failure that also appears without the patch is evidence against blaming this CL. A failure that repeats only with the patch is stronger regression evidence. LUCI Analysis can add stability information, and ResultDB records exonerations and test variants.</p><p>Re-running a failing test alone can change timing, order, and shared state, so the retry uses the same shard configuration first. The without-patch phase may need recompilation, which costs time but supplies a control group. In a large project this is more useful than a simple “red means reject” rule. When debugging a CQ result, save all three executions and their exact variants; the verdict is a conclusion derived from them.</p>`],
    ['Submission, numbering, and the tree are separate systems', `<p>LUCI CV owns the Run state and submission decision. Gerrit owns the merge and rebase; Chromium’s project uses a rebase-on-submit policy. The git-numberer plugin adds the <code>Cr-Commit-Position</code> footer when a change is submitted. There is no separate daemon handing out sequential numbers before merge. A Run can wait for tree status or another submission, then enter a submitting state that cannot simply be cancelled like a pending run.</p><p>After landing, scheduler and parent recipes fan work out to post-submit builders and testers. Tree-closing notifications use configured step and failure categories. Infrastructure failure is not necessarily product failure; a checkout step can fail before any source was tested. Gardeners and bisection services help identify and revert regressions. A good incident note names which service made each decision: CV, Gerrit, Buildbucket, Swarming, ResultDB, LUCI Analysis, notifier, or human gardener.</p>`],
    ['The infrastructure has deliberate costs', `<p>Source-controlled configuration is reviewable, but a change takes time to validate and import. Reusing equivalent tryjobs and deduplicating Swarming tasks saves compute, but requires careful identity rules. CQ retries and without-patch tests reduce false regression reports, but can compile or run more work. Branch selectors reuse one configuration language across main and release fleets, but increase the number of effective configurations engineers must reason about.</p><p>These are architectural tradeoffs, not accidental complexity. When proposing a CI improvement, state which cost it moves: developer latency, machine time, false rejections, missed regressions, or reviewability. Then find the config and runtime records that could measure the effect. The public source explains much of Chromium’s CI, while some internal performance and release services remain outside the collected archive’s direct evidence.</p>`]
  ],
  release: [
    ['The pinned cadence and branch identity', `<p>The checked-in release-cycle document at the studied September 2026 revision describes a two-week milestone development rhythm, a stabilization period before stable release, and weekly stable refreshes. Extended Stable keeps selected milestone branches alive longer. Earlier public pages describe older four-week or six-week cadences; cite a revision rather than treating a historical schedule as a law of Chromium.</p><p><code>chrome/VERSION</code> has major, minor, build, and patch fields. The build field identifies a branch-head lineage in the studied process; patch advances on a branch. Every trunk build number can have a branch-head ref, not only the headline milestone branch. To determine whether a fix shipped, use the full version and branch commit history. Calendar proximity between a main-branch commit and a release is not evidence of inclusion.</p>`],
    ['Release scripts turn known-good source into versioned refs', `<p>V8’s release scripts in the separate checkout illustrate the automation pattern. A builder advances a last-known-good ref; <code>create_release.py</code> reads a candidate, computes a new version, creates branch and tag refs, and records step state in JSON. The shared <code>common_includes.py</code> framework saves state after each step and permits a failed run to resume. The release script does not itself decide whether an arbitrary source revision is good; that decision was made by testing before the ref moved.</p><p>Chromium then receives a V8 release through a dependency roll, which itself needs integration testing. Some release work still needs human approval or promotion. The studied V8 checkout reports a different version from Chromium’s DEPS pin, so this script is a concrete example of the release mechanism rather than proof of exact pinned V8 behavior. The broader pattern is durable workflow state: branch creation, version update, tagging, and rollout should be individually observable and recoverable.</p>`]
  ]
};
