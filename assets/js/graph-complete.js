export const graphComplete = {
  graphs: [
    ['Start from SSA and ordered inputs', `<p><a class="inline-source" href="https://www.oracle.com/technetwork/java/javase/tech/c2-ir95-150110.pdf" target="_blank" rel="noopener noreferrer">Click and Paleczny’s 1995 paper</a> begins with a program already in static single assignment form: each named value is defined once. Its graph has labeled nodes and ordered input edges. Outputs are not ordered. For <code>a = b + c</code>, an ADD node has a first input from b and a second from c. The input order can matter for noncommutative operators, but there is no reason to store an arbitrary order among every consumer of a.</p><p>That choice removes instruction lists as the primary representation. If two pure operations have no dependency edge, the graph does not claim one must happen first. This is the essential idea behind “floating” nodes. A later scheduler still has to choose an executable order; the graph has merely avoided inventing one too early.</p>`],
    ['REGION, PHI, and projections encode control precisely', `<p>A control-flow join becomes a REGION node. A PHI’s first input names its REGION; its remaining value inputs line up by position with the REGION’s incoming control edges. That positional agreement says which value belongs to which predecessor without attaching a separate predecessor label to every value edge. If an optimizer rewrites a branch, it must preserve the alignment.</p><p>An IF node splits control according to a predicate. Rather than labeling outgoing graph edges “true” and “false,” the design treats a multi-output result as a tuple and uses small PROJECTION nodes to pick a component. This keeps edges lightweight and places meaning on nodes. The pattern recurs in systems design: if edges are numerous, moving metadata into a smaller set of typed nodes can simplify storage and traversal, provided the resulting graph remains readable.</p>`],
    ['Memory is an abstract value with a dependency chain', `<p>A real program reads and writes memory, so value and control edges alone are insufficient. The paper treats the current memory state as an abstract value. START produces the initial state; a STORE consumes a prior state, address, and value and produces a new state; a LOAD consumes a state and address and produces a data value. At branches, PHI can merge memory states just as it merges ordinary values.</p><p>This enforces order between operations that might affect one another. It is deliberately coarse when all memory is one value: unrelated stores can be ordered unnecessarily. Better alias analysis can split memory into independent regions or objects, but the 1995 paper names that possibility rather than solving it. When reviewing a compiler optimization, ask which alias assumption permits an effect edge to be removed. A wrong alias assumption changes program behavior.</p>`],
    ['What execution means without blocks', `<p>The paper gives control and data different semantics in one graph. Control behaves like a token moving through START, IF, REGION, and STOP. Data outputs are available as functions of their inputs, and changes propagate through dependent nodes. At a PHI, control chooses the matching incoming value. At a loop, a REGION and its PHIs tie the back edge to values produced on the previous iteration.</p><p>For a counted loop, draw the initial index, PHI, increment, condition, and back edge. Then mark the memory state if the body writes an array. This exercise prevents a common misunderstanding: “floating” does not mean “execute whenever.” It means placement is delayed while the graph still states every semantic dependence that a legal placement must respect.</p>`],
    ['The implementation and parse-time optimization', `<p>The paper’s C++ implementation used one class per opcode. A node’s virtual-table pointer identified its operation rather than storing a separate opcode field; virtual methods localized constant folding and other behavior by operation. The authors then tried optimization while constructing the graph. Before anyone used a newly created node, they asked whether it was a constant, an algebraic identity, or equivalent to an existing node with the same inputs. If so, the new node could be replaced immediately.</p><p>That peephole sees related nodes through graph edges even when their source instructions were far apart. It must remain conservative because later, not-yet-parsed code may change what a more global analysis can prove. The design lesson is not a magic local optimizer. It is that a representation exposing the right relationships can make cheap local work surprisingly effective.</p>`],
    ['What the paper measured, and what it did not', `<p>The paper tested 52 procedures across a dozen applications on an ILOC virtual machine. Parse-time peephole optimization reduced total compile time from 116.1 to 108.8 seconds in its experiment. Full global analysis then improved simulated execution cycles only modestly beyond the peephole-plus-scheduling result: 1.9% across the suite in the close reading’s aggregation. Those numbers describe that benchmark and machine model, not current V8 or a modern CPU.</p><p>The paper does not supply the final scheduling algorithm, solve aliasing, assess graph readability, or model a memory hierarchy. Its virtual-machine LOAD cost is not a modern cache/memory cost. V8’s later move toward Turboshaft shows that an IR’s maintenance and compile-time costs matter alongside optimization freedom. Use the paper to learn how to reason about representation; inspect current compiler code and measurements before judging a current pipeline.</p>`],
    ['Use the same reasoning outside a compiler', `<p>A build graph, a task scheduler, and a spreadsheet all encode dependencies and choose an order later. The transferable question is: which edges are real constraints, which are accidental order, and which side effects must be modeled? In a Chromium build, a generated header is a real dependency of a compile action. In a renderer, a pending navigation cannot commit before browser checks. In CI, tests depend on a materialized checkout and compiled binary. Omitting those edges is a correctness bug; adding arbitrary edges wastes parallelism.</p><p>Do not turn this into a slogan to “use graphs everywhere.” A graph is useful when its nodes and edges make a real invariant easier to inspect and enforce. If the graph hides ownership or causes expensive traversal, a different representation may be better. That is exactly the tradeoff the Sea of Nodes story teaches.</p>`]
]
};
