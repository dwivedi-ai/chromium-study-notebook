// Worked investigations for the parts where a short architecture summary is insufficient.
export const workshops = {
  owners: [
    ['A frame tree is not always the whole tab', `<p><code>docs/frame_trees.md</code> makes an important distinction: content and Blink each have a frame-tree representation. Content can nest a second frame tree inside a placeholder for features such as fenced frames or an embedded PDF viewer. Blink’s tree does not expose that placeholder in the same way. A <code>WebContents</code> can also have non-primary trees for prerendering or the back/forward cache. Only one page is primary and actively displayed, but other page state can coexist.</p><p>Now consider a fenced frame. Its inner document has a main frame because it is the root of its own tree, yet it is not the outermost main frame of the tab. The doc’s example shows why <code>RenderFrameHost::GetParent()</code> and <code>GetParentOrOuterDocument()</code> answer different questions. If you are writing code that walks “all frames,” decide whether it should cross a nested-tree boundary. Privacy and embedding rules may make that distinction essential.</p><p>For a source investigation, draw both the content tree and the Blink-visible tree for one fenced frame. Mark the placeholder, inner main frame, and primary page. Then check the traversal API used by the code you are studying. The visible tab, a page, and a frame tree have different boundaries.</p>`],
    ['What changes when a frame navigates', `<p>The content frame tree consists of <code>FrameTreeNode</code> objects. A node represents a position in the frame structure and can outlive a particular document. Its current <code>RenderFrameHost</code> can change when a navigation creates a new document. This is a useful ownership model: stable slot, replaceable document host. The document’s Blink objects and V8 context belong to the renderer side and have their own teardown.</p><p>Suppose a child iframe navigates from A to B. Do not ask only “what is the iframe pointer?” Ask whether the frame-tree node stayed, which <code>RenderFrameHost</code> is current, where the previous host went, and which renderer owns the new DOM. A bug that stores a host pointer as if it were the permanent frame identity may act on the old document after commit. The checked-in frame-tree document warns that a host is only roughly one-to-one with a document; <code>docs/render_document.md</code> explains the caveat. Record the revision and lifecycle state when making a stronger claim.</p>`]
  ],
  navigation: [
    ['Two overlapping navigations are possible', `<p>The simple diagram “pending → commit” hides concurrency. <code>docs/navigation_concepts.md</code> says a frame can have a cross-document request awaiting a final response, plus requests that are ready to commit while waiting for renderer acknowledgement. Ownership changes: a <code>FrameTreeNode</code> owns the request during the response stage, while a <code>RenderFrameHost</code> can own a ready-to-commit request. Same-document navigations use shorter paths.</p><p>Imagine a slow request to B, followed by a user navigation to C. The browser must decide whether B is cancelled, whether any ready-to-commit work remains, and which renderer receives the commit. A web page should not be able to defeat a user’s browser-initiated navigation by continuously starting renderer-initiated requests. That is why cancellation rules consider the initiator, not just “latest request wins.”</p><p>When debugging, log navigation ID, initiator, frame-tree-node ID, owner, state, and commit acknowledgement. A URL alone cannot distinguish the overlapping requests. Draw each request on its own timeline and note exactly when its owner changes.</p>`],
    ['Why the address bar sometimes shows a pending URL', `<p>A typed browser navigation may show its pending destination while the old page is still visible. A renderer-initiated navigation usually leaves the old committed URL visible, because the renderer controls the old page’s content and could otherwise make attacker content appear under a victim URL. <code>docs/navigation_concepts.md</code> gives a narrow exception for a new, unmodified tab and explains when the visible URL returns to <code>about:blank</code>.</p><p>This is a security design, not merely UI polish. The browser must avoid showing a trusted-looking URL beside content that has not committed from that origin. The current document’s authority still comes from its last committed origin. If a navigation fails, the pending URL may disappear without ever becoming the document URL. In a test, vary the initiator: type a URL, click a link, and set <code>location.href</code> from script. Observe the visible URL during a delayed response and compare it with the committed origin in browser-side state.</p>`]
  ],
  parsing: [
    ['Read generated code as part of the source', `<p>Generated identifiers can feel invisible when you search only checked-in C++ files. For Blink HTML and CSS, the source of truth is JSON5 plus generator code. The output is still real implementation: it defines enums, lookup functions, factories, and property metadata compiled into the browser. A useful archaeology path is input row → generator rule → generated file in <code>out/...</code> → runtime caller → test.</p><p>For an HTML tag, inspect whether the row has an explicit interface name or runtime flag. Predict its factory class, then compare with the generated factory. For a CSS property, find its definition and ask which generated ID it gets and whether exposure is conditional. Do not fabricate a header from memory when a build output can answer exactly. The supplied research generated these outputs at the pinned revision; the sparse local checkout does not contain them.</p>`]
  ],
  lifetime: [
    ['A concrete teardown investigation', `<p>Consider an asynchronous operation started by a document: it schedules work, then the user navigates away. Draw three timelines: the document’s lifetime, the task’s queued/running state, and the managed object’s reachability. The object might remain reachable after detach because a persistent root or another owner still refers to it. The task might be cancelled before it runs, or it might run and detect that the execution context is gone.</p><p>At the posting site, record the captured values and task runner. At the callback, record the first dereference and any early-return guard. At teardown, record which callbacks or requests are cancelled. Finally read a test that destroys a document while the operation is pending. The desired result may be “no callback,” “reject promise,” or “finish work without touching the old page”; those are different contracts.</p><p>Oilpan only answers whether traced objects can be reclaimed. Sequence checks answer where code may run. Document lifecycle checks answer whether the operation still belongs to the active page. A complete review proves all three separately.</p>`]
  ],
  graphs: [
    ['Work through a branch and a memory write', `<p>Consider <code>if (flag) x = 1; else x = 2; obj.value = x; return obj.value;</code>. A graph needs a control split for the condition and a join for the two paths. A PHI at the join receives the value from the matching incoming path. The store consumes that PHI value. The final load must be ordered after the store by an effect dependency, even if it does not take the store’s ordinary numeric result as input.</p><div class="code-path"><b>Required relationships</b><code>flag → IF → true/false control → REGION<br>1, 2 + matching control → PHI → STORE(value)<br>STORE(effect) → LOAD(effect) → return</code></div><p>Now remove the effect edge from store to load. A scheduler may legally place the load first under the remaining graph, producing the old property value. Remove the control alignment at the PHI and the wrong branch’s value may be selected. This is how to review an intermediate representation: identify which semantic fact each edge preserves, then ask what illegal schedule becomes possible if it is absent.</p>`],
    ['What the old paper can and cannot tell you', `<p>The 1995 Sea of Nodes paper is useful because it removes an artificial basic-block ordering and asks which dependencies are truly required. Its REGION and PHI structure gives a precise model for joins; effect edges let memory participate in the same graph. The paper does not prove that every later compiler should use the same representation. Alias analysis, scheduling quality, compile time, and cache locality remain separate problems.</p><p>The supplied V8 writing explains that V8 later moved much optimization work toward Turboshaft’s more explicit control-flow form. The engineering lesson is to choose a representation for the dominant operations and the engineers who must maintain it. When studying current V8, first identify which pipeline and IR a function actually uses. Do not project TurboFan’s historical Sea of Nodes implementation onto every compiler phase in the separate V8 checkout.</p>`]
  ],
  release: [
    ['A worked inclusion check', `<p>Suppose a crash fix lands on main on Tuesday and a stable build ships Friday. Begin with the full stable version, not the calendar dates. Read its branch-head and build revision. Search that branch for the fix or a cherry-picked equivalent. If present, compare the branch’s dependency pins and any build flags relevant to the crash. If absent, the main commit cannot explain the stable binary. If present but behind a disabled feature, source inclusion still does not prove the user path was active.</p><p>The same method works in reverse for regressions. Identify the first bad full version, map it to source revision and branch, then compare with the last good build. Narrow the commit range before reading individual patches. If a dependency roll lies in the range, inspect the child repository changes too. The website’s dependency, CI, and release chapters are connected because real regression diagnosis crosses all three.</p>`]
  ],
  independent: [
    ['What a strong investigation should contain', `<p>A finished investigation should let another engineer reproduce the reasoning. State the observed behavior and exact environment. Draw process, thread, and object ownership. Give a call path with the task posts and IPC crossings included. Name one invariant and the code or test that enforces it. Show an experiment with a prediction, result, and interpretation. Then list open questions and the next check for each.</p><p>A weak note says “NavigationRequest handles navigation.” A useful note says which navigation initiated it, which object owned the request at each state, which renderer received commit, which test covers cancellation, and what a trace showed in one run. The second note can be challenged and improved. That is the standard to aim for when studying an unfamiliar subsystem.</p><p>For a self-check, remove your prose and look only at paths, diagram, test, and observation. Could someone reconstruct the main claim? If not, add the missing evidence. Then remove the paths and read the explanation alone. Does it say what the system does and why its boundaries exist? Both sides matter.</p>`]
  ]
};
