import { supplemental } from './supplemental.js';
import { systems } from './systems.js';
export const revision = 'f288fed6c601ae22461c9c9d8189004050abfaeb';
export const source = (path, why, repo='src', local=true) => ({path, why, repo, local});
const baseLessons = [
  {
    id:'evidence', n:'01', part:'Getting started', title:'How to study the Chromium source code', deck:'Use documentation, implementation, tests, and experiments together.', deps:[], skill:'Reading source',
    lead:'This course uses Chromium source code as its main reference. Before looking at any subsystem, you need to know what is in the supplied checkout, how to find an implementation, and how to check whether an explanation matches the code.',
    flow:['Start with a question','Read the design document','Find the implementation','Read a test','Check the behavior'],
    sections:[
      ['What each source can tell you',`<p>A design document explains what a component is intended to do. A source file shows how one revision implements it. A test shows behavior that developers chose to check. A trace or debugger shows what happened in one run. These sources answer different questions, so use more than one for important claims.</p><p>For example, <code>content/README.md</code> explains why web-platform code belongs in <code>content</code>. To learn how a particular request crosses from Chrome into content, you would then find the interface, follow its implementation, and read a relevant test.</p>`],
      ['What is available in this archive',`<p>This course was reconstructed from five Chromium research writings, three research dossiers, a Chromium checkout, a separate V8 checkout, and depot_tools. The checkouts are sparse: many Blink and content implementation files cited by the research are not present locally. The source links in each chapter say when a file is available here and when it must be read at the pinned upstream revision.</p><p>The main Chromium checkout is at <code>f288fed6</code>. The separate V8 checkout reports 15.6.0, while this Chromium revision pins V8 15.6.19. Keep those versions separate when checking compiler behavior.</p>`],
      ['A practical way to read a subsystem',`<p>Start with a behavior you can describe, such as “a cross-site iframe loads in another renderer.” Read the nearest overview document, find the objects involved, follow one call path, and look for a test. Write down which parts are confirmed by code and which are still your interpretation. This small record prevents an early guess from turning into a false mental model.</p>`]
    ],
    quiz:['A dossier cites a class in a file missing from this sparse checkout. What can you claim?', 'You can report that the supplied research cites the class. You have not verified the implementation locally. Fetch the exact file at the pinned revision or mark the claim as research cited.'],
    lab:{title:'Find where a feature belongs', aim:'Use the source tree to understand the boundary between Chrome and content.', prediction:'The content layer contains generic browser behavior; Chrome adds product features through interfaces.', steps:['Read the “content vs chrome” and “Content API” sections of content/README.md.','Search for ContentBrowserClient and inspect its declaration.','Write down one feature that the README places in Chrome and how content can call into the embedder.'], observation:'The README gives the code-placement rule, and the interface shows one way Chrome supplies behavior to content.', conclusion:'A directory boundary has a concrete API. Reading both the policy and the API gives a better explanation than either alone.', follow:'Which build rule prevents content from depending directly on Chrome?'},
    files:[source('content/README.md','Module policy'),source('docs/README.md','Documentation entry point'),source('docs/dependencies.md','Git, CIPD, and GCS dependencies')]
  },
  {
    id:'checkout', n:'02', part:'Source and build', title:'How Chromium gets its source and dependencies', deck:'Understand DEPS, gclient sync, packages, and generated build settings.', deps:['evidence'], skill:'Checkout and build inputs',
    lead:'Chromium is not built from the main src repository alone. It also needs pinned versions of V8, third-party libraries, platform packages, and tools. This chapter explains how a checkout obtains them and why two machines at the same src commit can still differ.',
    flow:['.gclient selects src','DEPS lists dependencies','gclient sync resolves them','Files and packages arrive','Hooks prepare the build'],
    sections:[
      ['What DEPS and gclient do',`<p><code>src/DEPS</code> lists other repositories and packages Chromium needs. A local <code>.gclient</code> file tells depot_tools which top-level source tree to manage. <code>gclient sync</code> evaluates those inputs for this checkout and fetches the selected revisions. A <code>git pull</code> of <code>src</code> alone cannot reproduce that work.</p><p>Before reading the implementation, predict whether two machines at the same <code>src</code> commit must fetch identical dependencies. The answer depends on host conditions and checkout configuration; the next sections show where those inputs enter.</p>`],
      ['What happens during sync',`<p><code>depot_tools/gclient.py</code> coordinates work that happens at different times: evaluate the manifest, fetch eligible repositories and packages, record managed paths, write generated build inputs, and run configured hooks. If a sync fails, find the first failed phase. A parser error, a missing Git revision, and a hook error have different causes even when the command prints the same final “sync failed” message.</p><p>The following section follows that queue and its post-sync work in source. Keep the phase order beside any error log you investigate.</p>`],
      ['How dependencies affect the build',`<p>Sync can also write <code>build/config/gclient_args.gni</code> from variables named in DEPS. GN build files import those generated values. For example, <code>build/toolchain/siso.gni</code> reads a generated setting when deciding whether Siso is the default build tool. To reproduce a build, check the src revision, dependency revisions, generated files, host platform, and local <code>args.gn</code>.</p>`]
    ],
    quiz:['Two machines have the same src commit but different build behavior. Is the commit pin broken?', 'Not necessarily. Compare DEPS evaluation, generated gclient_args.gni, args.gn, toolchain pins, and host conditions.'],
    lab:{title:'Follow one setting from DEPS into GN', aim:'See how checkout configuration changes a build decision.', prediction:'The value of build_with_chromium reaches Siso build logic through a file generated by gclient.', steps:['Search src/DEPS for gclient_gn_args and build_with_chromium.','Read the import at the top of build/toolchain/siso.gni.','Identify the controlled DEPS input, generated GN file, and local build args.'], observation:'The Siso GN file imports gclient_args.gni, which gclient produces from selected DEPS variables.', conclusion:'A source revision alone is not the complete input to a build.', follow:'How does a CI bot make sure the generated file matches the revision under test?'},
    files:[source('DEPS','Dependency graph'),source('gclient.py','Sync logic','depot'),source('gclient_eval.py','Restricted DEPS parser','depot'),source('tests/gclient_eval_unittest.py','Parser behavior and constraints','depot'),source('build/toolchain/siso.gni','Generated args consumed by build'),source('tools/clang/scripts/update.py','Clang pin and downloader','src',false),source('recipes/recipe_modules/bot_update/api.py','CI checkout recipe','depot',false)]
  },
  {
    id:'owners', n:'03', part:'Browser architecture', title:'Chromium processes and major components', deck:'Learn what the browser, renderers, Blink, V8, cc, and Viz each do.', deps:['checkout'], skill:'Processes and ownership',
    lead:'When a page opens, work is divided among processes and components. The browser process manages navigation and policy; renderer processes run web content; other parts handle compositing and display. Understanding who owns each piece of state makes later chapters much easier.',
    flow:['Browser: frame + navigation','Renderer: Blink + V8','Renderer: cc','GPU: Viz','Display'],
    sections:[
      ['The browser and renderer processes',`<p>The browser process keeps the tab and frame structure, coordinates navigation, and makes decisions that web content must not control. A renderer process runs a page’s HTML, CSS, and JavaScript. Separating them helps contain crashes and keeps untrusted page code away from browser privileges.</p><p>The <code>content</code> module implements the core multiprocess browser. Blink implements web-platform features inside the renderer, and V8 executes JavaScript. <code>cc</code> manages compositor state, while Viz combines compositor frames for display. The <code>chrome</code> directory adds product features such as extensions and autofill around the content layer.</p>`],
      ['Tabs, frames, and processes are different',`<p>A <code>WebContents</code> is a browser-side object for tab-like content. A page has a frame tree: a main frame and possibly child frames. Browser-side <code>RenderFrameHost</code> objects represent frames that may be hosted by renderer processes. Site isolation can place a cross-site iframe in a different renderer from its parent.</p><p>That is why “one tab equals one process” is unreliable. The tab is a user-interface concept; frames are document containers; a renderer process is an operating-system process that may host one or more frames. Read <code>docs/frame_trees.md</code> and <code>docs/process_model_and_site_isolation.md</code> with those three identities in mind.</p>`],
      ['What survives a renderer crash',`<p>If a renderer crashes, its DOM objects and JavaScript execution state disappear. The browser process still has its own representation of the tab and frame tree, so it can report the crash or start another navigation. To understand any recovery path, list the state stored in the renderer and the state retained by the browser.</p>`]
    ],
    quiz:['A page has a cross-site iframe. Must its DOM share the outer frame’s renderer?', 'No. Site isolation can place cross-site frames in separate renderers. The browser coordinates the frame tree; each renderer owns its DOM state. Check revision-specific policy and exceptions.'],
    lab:{title:'Locate the objects behind a page', aim:'Identify which process owns a tab, frame, document, and compositor tree.', prediction:'Browser navigation state and renderer DOM state have different lifetimes.', steps:['Read docs/frame_trees.md and docs/process_model_and_site_isolation.md.','Find WebContents and RenderFrameHost declarations in content/public/browser.','Write a small table with each object, its process, and what happens to it after a renderer crash.'], observation:'The docs distinguish browser-side frame hosts from renderer-side document objects.', conclusion:'A page spans several objects and sometimes several processes.', follow:'Which objects change when a navigation commits a new document?'},
    files:[source('docs/process_model_and_site_isolation.md','Process placement'),source('docs/frame_trees.md','Frame tree'),source('docs/render_document.md','Document and RenderFrameHost relationship'),source('content/README.md','Module boundary'),source('cc/README.md','Compositor ownership'),source('third_party/blink/renderer/README.md','Blink dependency direction','src',false),source('third_party/blink/public/README.md','Blink embedder API','src',false),source('content/renderer/render_thread_impl.cc','Renderer startup and Blink initialization','src',false),source('components/viz/README.md','Display compositor role')]
  },
  {
    id:'navigation', n:'04', part:'Browser architecture', title:'How Chromium navigates to a page', deck:'Follow a request from its start through response checks and document commit.', deps:['owners'], skill:'Navigation lifecycle',
    lead:'Navigation begins when the user types a URL, clicks a link, or script requests a new page. Chromium starts a request while the old document may still be active. Only after checks and a successful commit does the new document take its place.',
    flow:['Start','Pending request','Response + policy','Commit','New document active'],
    sections:[
      ['The three URLs you may see',`<p>The <em>last committed URL</em> belongs to the document currently in the frame. A <em>pending URL</em> belongs to a navigation in progress. The <em>visible URL</em> is what the address bar displays; Chromium chooses it under rules meant to avoid misleading the user. These values can differ during a navigation. <code>docs/navigation_concepts.md</code> explains when each should be used.</p><p>For example, a renderer can request a new URL while the old document remains visible. Code checking the current document’s permissions must use its committed origin, not the destination that is still pending.</p>`],
      ['From request to commit',`<p>A same-document navigation, such as changing a fragment, keeps the existing document. A cross-document navigation creates a replacement document. Chromium also distinguishes navigations started by the browser from those started by a renderer, because the initiator affects what requests may be trusted.</p><p>The browser-side <code>NavigationRequest</code> tracks work before commit, and a <code>RenderFrameHost</code> is involved in hosting the resulting document. Follow the navigation timeline in <code>docs/navigation.md</code>; then read the pinned implementation file upstream, because it is absent from this sparse checkout.</p>`],
      ['What cancellation means',`<p>A request can fail, redirect, or be replaced by another request before it commits. In those cases, the previous committed document may remain the current document. Draw this case explicitly: old document active → new request pending → request cancelled → old document still active. It prevents the common mistake of treating a started navigation as a completed one.</p>`]
    ],
    quiz:['Before commit, which origin should a feature use for the current document’s security context?', 'The frame’s last committed origin, rather than the pending or visible URL. The navigation concepts document separates these values and explains their security roles.'],
    lab:{title:'Draw a navigation that gets cancelled', aim:'Check what happens to the current document when a new request does not commit.', prediction:'The old document remains current if the pending navigation is cancelled before commit.', steps:['Read the URL definitions in docs/navigation_concepts.md.','Read docs/navigation.md and mark the request start and commit points.','Draw separate browser and renderer timelines, then cancel the request before commit.'], observation:'Pending and committed state remain distinct; even an address-bar URL may change back on cancellation.', conclusion:'Starting a navigation does not make a new document active.', follow:'Find a navigation test that cancels or redirects a renderer-initiated request.'},
    files:[source('docs/navigation_concepts.md','URL semantics'),source('docs/navigation.md','Navigation timeline'),source('docs/render-frame-host-lifecycle-state.gv','Host lifecycle'),source('content/browser/renderer_host/navigation_request.cc','Browser navigation state','src',false),source('third_party/blink/renderer/core/loader/frame_loader.cc','Blink navigation entry point','src',false),source('third_party/blink/renderer/core/loader/document_loader.cc','Blink document commit','src',false),source('content/renderer/render_thread_impl.cc','Blink renderer initialization','src',false)]
  },
  {
    id:'time', n:'05', part:'Threads and rendering', title:'Tasks, threads, and the renderer scheduler', deck:'See how Chromium chooses when callbacks, script, and rendering work run.', deps:['navigation'], skill:'Task scheduling',
    lead:'The renderer has work from many sources: user input, network responses, JavaScript, timers, and frame updates. Chromium puts work on task queues and uses scheduling policy to decide what runs next. This chapter explains the main thread, task posting, and the timing of a frame.',
    flow:['Select task','Run script / callback','Microtask checkpoint','Rendering opportunity','Next task'],
    sections:[
      ['What a task queue does',`<p>A task is a unit of work scheduled to run later on a thread or sequence. Chromium uses task runners to post callbacks instead of calling them immediately. The renderer has several kinds of work, and Blink’s scheduler can give different queues different priority. This helps user input remain responsive while pages also load resources and run script.</p><p>“Runs on the main thread” tells you where the callback executes. It does not tell you when it will run. Read <code>docs/threading_and_tasks.md</code> for Chromium’s task primitives and the renderer scheduler source for Blink-specific policy.</p>`],
      ['How to follow an asynchronous call',`<p>Suppose code posts a callback to a task runner. The posting function returns before the callback runs, so a normal stack trace will not show the complete path. Find both the <code>PostTask</code> call and the callback. Record the task runner, the object captured by the callback, and the condition under which the work should be cancelled.</p><p>For a cross-process operation, add the IPC sender and receiver. The resulting sequence shows the causal path even though no single call stack contains it all.</p>`],
      ['When rendering can happen',`<p>Promise reactions run at microtask checkpoints, including defined script-scope and task-completion boundaries. A rendering opportunity may later allow Blink to update style, layout, and paint; painting does not occur after every task. The compositor can do some work on another thread. A long task on the renderer main thread can still delay a newly changed page from reaching the screen.</p>`]
    ],
    quiz:['Does the browser necessarily paint between an event handler and a Promise reaction it schedules?', 'No. Promise reactions run at a microtask checkpoint before an ordinary next rendering opportunity. Verify the exact Chromium scheduling path for a particular case.'],
    lab:{title:'Follow a posted callback', aim:'Find both ends of an asynchronous operation.', prediction:'The function that posts work and the callback that runs it appear in different parts of the call path.', steps:['Read docs/threading_and_tasks.md and docs/threading_and_tasks_testing.md.','Find a PostTask call in an available cc source file. Identify its callback and task runner.','Write down what object the callback uses and whether that object can disappear before the task runs.'], observation:'The posting site shows when work is queued; the callback body shows what runs later.', conclusion:'Understanding asynchronous code requires the scheduling point, the execution point, and the object lifetime.', follow:'Which test runner lets you control when the task executes?'},
    files:[source('docs/threading_and_tasks.md','Task primitives'),source('docs/threading_and_tasks_testing.md','Task tests'),source('third_party/blink/renderer/platform/scheduler/','Renderer scheduler','src',false),source('third_party/blink/renderer/platform/scheduler/public/event_loop.h','Blink event-loop boundary','src',false),source('content/renderer/render_widget.cc','Renderer input and widget','src',false)]
  },
  {
    id:'pixels', n:'06', part:'Threads and rendering', title:'How Blink turns a page into pixels', deck:'Follow DOM and CSS through style, layout, paint, compositing, and display.', deps:['time'], skill:'Rendering pipeline',
    lead:'A browser does not draw the DOM tree directly. Blink first resolves CSS, computes element geometry, and records painting information. The compositor and Viz then turn that information into a frame on screen. Each stage has a different job and a different kind of output.',
    flow:['DOM + CSS','Style','Layout','Pre-paint + paint','cc commit','Raster + Viz'],
    sections:[
      ['Style, layout, and paint',`<p>The DOM records the document’s nodes. CSS rules are matched and resolved into computed style. Layout uses that style and the available space to decide sizes and positions. Paint records what should be drawn, such as text, backgrounds, and borders. Changing a CSS color may require new paint work without changing layout; changing an element’s width may require layout as well.</p><p>Separating these results lets Chromium avoid repeating work when an earlier stage has not changed. It also gives you a way to diagnose a visual bug: check the first stage whose output is wrong.</p>`],
      ['Commit, raster, and display',`<p>The <code>cc</code> compositor receives updated layer and property-tree state at a commit. <code>cc/README.md</code> describes a pending compositor tree that can be prepared while the active tree remains available for drawing. When the pending tree is ready, activation makes it the active tree. Raster work turns recorded content into pixels; Viz combines compositor frames for display.</p><p>Paint and final draw are different operations. A correct paint record can still produce a stale screen if the new state has not committed, rasterized, activated, or drawn. The <code>LayerTreeHost</code> and scheduler sources are the next places to read.</p>`],
      ['Using the stages to debug',`<p>For a wrong font or color, inspect style and paint. For an element in the wrong position, inspect layout. If style, geometry, and paint records look right but the screen does not update, inspect compositor commit, damage, raster, and activation. This stage-by-stage approach gives a concrete starting point instead of searching every rendering file.</p>`]
    ],
    quiz:['A transform animates while the main thread is busy. What does that suggest?', 'It suggests compositor-side state can update that particular animation without new layout each frame. It does not prove every transform animation is eligible or that main-thread setup and commit are irrelevant.'],
    lab:{title:'Compare two kinds of animation', aim:'See which rendering stages a change requires.', prediction:'A transform animation may update compositor state without running layout on every frame; a width animation usually needs geometry work.', steps:['Read cc/README.md and docs/how_cc_works.md.','Find where LayerTreeHost requests a commit and read the scheduler state-machine test.','With a runnable Chromium build, record Performance traces for a transform animation and a width animation. Compare Layout, Paint, and compositor activity.'], observation:'The traces should show whether each animation caused layout and paint in that run. Eligibility and fallback can vary.', conclusion:'The first stage that changes explains much of the performance difference.', follow:'Which conditions keep an animation on the compositor?'},
    files:[source('cc/README.md','Compositor model'),source('docs/how_cc_works.md','Compositor architecture'),source('cc/trees/layer_tree_host.cc','Main-thread host'),source('cc/scheduler/scheduler_state_machine.cc','Scheduling decisions'),source('cc/scheduler/scheduler_state_machine_unittest.cc','Scheduler invariants in tests'),source('third_party/blink/renderer/core/dom/document_lifecycle.h','Blink lifecycle','src',false),source('third_party/blink/renderer/platform/graphics/paint/paint_artifact.h','Paint output','src',false),source('third_party/blink/renderer/core/css/style-calculation.md','Style matching and invalidation','src',false)]
  },
  {
    id:'bindings', n:'07', part:'Web APIs and memory', title:'How JavaScript calls Blink web APIs', deck:'Understand Web IDL, generated bindings, V8 contexts, and C++ implementations.', deps:['pixels'], skill:'Web API bindings',
    lead:'When JavaScript calls a DOM method, V8 must expose the method, convert its arguments, and call Blink’s C++ implementation. Web IDL describes much of this interface, and Blink generates binding code from it. The path is longer than a direct JavaScript-to-C++ call.',
    flow:['HTML / CSS / IDL','Generated lookup + bindings','Blink object','V8 context','Web-visible result'],
    sections:[
      ['What Web IDL describes',`<p>Web IDL files declare web-visible interfaces, methods, attributes, argument types, and exposure rules. Blink’s binding generators use these declarations to produce code that presents objects to V8 and converts values between JavaScript and C++. CSS property metadata plays a similar role for parts of the CSS implementation.</p><p>This generation avoids hand-writing the same conversion and exposure logic for thousands of APIs. To understand a web method, begin with its IDL declaration, then find the generated binding convention and the C++ method it calls.</p>`],
      ['From JavaScript to Blink',`<p>A page uses a V8 context associated with a frame and execution world. When script accesses a DOM object, a V8 wrapper connects the JavaScript object to a Blink object. A generated binding checks and converts the arguments, then invokes Blink implementation code. The result is converted back to a JavaScript value or an exception.</p><p>Feature flags and exposure rules can make an API unavailable in a particular context. A C++ method with the right name does not, by itself, prove that a page can call it.</p>`],
      ['When an API needs browser-process work',`<p>Some web APIs need a network request, permission check, or other operation outside the renderer. In that case the Blink method is only the beginning of the path. Follow its call through content or Mojo to the browser-side endpoint, and check which side validates the request. The JavaScript interface does not grant the renderer browser-process authority.</p>`]
    ],
    quiz:['You find a C++ method with the right name. Have you found the web API entry point?', 'Not yet. Locate IDL, generated binding path, exposure rules, and runtime feature conditions. The method may be internal or unreachable in a particular context.'],
    lab:{title:'Trace one DOM method', aim:'Find the declaration, binding, implementation, and test for a web API.', prediction:'The IDL declaration reveals the web-visible name and types before generated C++ is inspected.', steps:['Choose a familiar DOM method, such as Element.closest().','Find its .idl declaration and its C++ implementation in the pinned upstream Blink tree.','Find a web test for the method and note what behavior it checks.','Check whether a runtime flag or exposure rule affects where it is available.'], observation:'You should find a path from IDL through binding behavior into implementation and a web test.', conclusion:'The public API, generated conversion code, and implementation are separate parts of the same feature.', follow:'Does this API ever call into the browser process?'},
    files:[source('third_party/blink/README.md','Blink policy'),source('third_party/blink/renderer/bindings/','IDL generation','src',false),source('third_party/blink/renderer/core/','DOM implementation','src',false),source('third_party/blink/web_tests/','Behavior tests','src',false),source('third_party/blink/renderer/platform/runtime_enabled_features.json5','Feature status and exposure','src',false),source('third_party/blink/renderer/core/html/html_tag_names.json5','Generated HTML tags','src',false),source('third_party/blink/renderer/core/css/css_properties.json5','Generated CSS properties','src',false),source('third_party/blink/renderer/bindings/scripts/','Web IDL compiler','src',false)]
  },
  {
    id:'lifetime', n:'08', part:'Web APIs and memory', title:'How Blink manages object lifetimes', deck:'Learn how Oilpan, V8 wrappers, managed pointers, and callbacks interact.', deps:['bindings','time'], skill:'Object lifetimes',
    lead:'A DOM object may be referenced by C++ objects, JavaScript objects, or both. Blink uses Oilpan and V8 garbage collection to manage these objects. You still need to understand which references keep an object alive and what happens when its document goes away.',
    flow:['JavaScript root','V8 wrapper','Blink heap object','Member<> edge','Other object'],
    sections:[
      ['Why Blink uses tracing garbage collection',`<p>DOM objects form graphs with cycles: a parent refers to children, children can refer to parents, and JavaScript can hold references to either. Simple reference counting does not automatically collect cycles. Oilpan, built on cppgc, traces reachable Blink objects. V8 separately manages JavaScript objects, so wrappers and cross-heap references must be handled correctly.</p>`],
      ['Strong and weak references',`<p>A <code>Member&lt;T&gt;</code> field is a managed reference inside the cppgc heap. A <code>WeakMember&lt;T&gt;</code> does not keep its target alive in the same way. A persistent handle can provide a root outside an ordinary heap object. Read <code>v8/include/cppgc/member.h</code> and <code>persistent.h</code>, then find a Blink class’s <code>Trace</code> method to see which fields the collector visits.</p><p>When studying a pointer, also find the owner of the containing object and the event that tears it down. A type alone does not tell you the whole lifetime.</p>`],
      ['Callbacks after a document goes away',`<p>Suppose a callback is posted while a page is loading, then navigation replaces the document before the callback runs. The callback must either keep the needed objects alive, notice that they are no longer valid for the task, or be cancelled. An object can still be in memory even when acting on it would be wrong for the new page. Check both memory safety and whether the work still belongs to the current document.</p>`]
    ],
    quiz:['A callback captures a DOM pointer and runs after navigation. Is it safe because Oilpan exists?', 'No. Prove that the edge is traced or rooted, then prove the callback still has the right document and state. Memory safety does not establish lifecycle correctness.'],
    lab:{title:'Follow a Blink object’s references', aim:'Work out what keeps one object alive and when it can be collected.', prediction:'A Member field is visible to tracing, but a posted callback has its own lifetime requirements.', steps:['Read the comments in v8/include/cppgc/member.h and persistent.h.','Find a Blink class with Member fields and a Trace method in the pinned source.','Draw the references into the object and look for callbacks that capture it.','Check what happens to those references when its document is destroyed.'], observation:'The member types and Trace method show collection edges; callback and teardown code show timing.', conclusion:'A complete lifetime explanation includes both references and the sequence of events.', follow:'What test could show that the object is released after navigation?'},
    files:[source('include/cppgc/member.h','Managed pointer','v8'),source('include/cppgc/persistent.h','Roots','v8'),source('third_party/blink/renderer/platform/heap/member.h','Blink managed pointer aliases','src',false)]
  },
  {
    id:'graphs', n:'09', part:'V8 and compiler design', title:'How V8 represents programs as graphs', deck:'Learn the Sea of Nodes idea and why value, control, and effect edges matter.', deps:['time'], skill:'Compiler graphs',
    lead:'A compiler needs a representation of a program before it can optimize it. The Sea of Nodes representation stores operations as graph nodes and records which operations depend on which others. V8’s TurboFan used this approach; this chapter explains both the idea and its limits.',
    flow:['JavaScript operation','Value dependencies','Control dependencies','Effect dependencies','Legal schedule'],
    sections:[
      ['What the graph records',`<p>Many compilers group instructions into basic blocks and keep each block’s instructions in a list. That list can impose an order even when two pure operations are independent. In a Sea of Nodes representation, an operation is a node and its input edges say what it needs. If a multiplication’s inputs are ready and no other dependency constrains it, an optimizer may move it.</p><p>The graph must still represent every ordering that affects program meaning. “No instruction list” does not mean arbitrary execution order.</p>`],
      ['Value, control, and effect edges',`<p>A value edge says an operation needs another operation’s result. A control edge says an operation belongs on a particular execution path. An effect edge orders operations that interact with memory or the outside world. For example, two writes to <code>o.x</code> may have no numeric value dependency, but reversing them changes the object’s final value.</p><p>At a branch join, PHI nodes select a value associated with the incoming control path. The graph example below develops this mechanism. The separate V8 checkout is sparse and does not contain the compiler implementation files cited by the research.</p>`],
      ['Why this representation helps and costs',`<p>When the graph records only the necessary dependencies, optimizers can see which operations may move or disappear. Later compiler stages must turn that partial order into a legal instruction schedule. That extra scheduling work is the cost of the flexibility. The same design question appears elsewhere in systems engineering: what information should a representation store explicitly, and what should be computed later?</p>`]
    ],
    quiz:['Can two writes to one object float independently if neither consumes the other’s numeric output?', 'No. Their effect order determines observable state; an IR must represent that dependency even without an ordinary value edge.'],
    lab:{title:'Model two writes to memory', aim:'See why a compiler graph needs effect dependencies.', prediction:'A graph with value edges alone permits the writes to run in the wrong order.', steps:['Write: o.x = 1; o.x = 2; return o.x.','Draw nodes and only the value edges. List an execution order that graph would allow.','Add effect edges between the two stores and the final load. Compare the allowed orders.','Compare your graph with the branch-join explanation in this chapter.'], observation:'The first graph permits an order that changes the returned value; the effect chain excludes it.', conclusion:'The graph must record memory order as well as ordinary data flow.', follow:'How does a PHI node match values to incoming control paths?'},
    files:[source('include/v8-version.h','Standalone V8 revision','v8'),source('src/compiler/','TurboFan implementation','v8',false)]
  },
  {
    id:'factory', n:'10', part:'Builds and releases', title:'How Chromium builds and tests changes', deck:'Follow a change through LUCI builders, test machines, the commit queue, and release branches.', deps:['checkout'], skill:'CI and release flow',
    lead:'Chromium changes are reviewed, built, tested, and submitted through several infrastructure services. The configuration for much of that work lives in the source tree. This chapter explains what a builder is, what runs on a bot, and how a passing change reaches the main branch.',
    flow:['Upload change','Commit queue selects jobs','Buildbucket creates builds','Swarming runs tasks','Results allow submission'],
    sections:[
      ['Builder, build, and task',`<p>A <em>builder</em> is a named configuration. A <em>build</em> is one execution of it in Buildbucket. A Swarming <em>task</em> is scheduled work on a bot. The difference matters when debugging: a declaration may be correct while one build receives unexpected properties, or a build may be healthy while one task fails on a machine.</p><p>The research counted 1,211 public builders at this revision. That number indicates the size of the configuration surface, not a stable fleet size. Pick one builder and expand its inherited settings before drawing conclusions about the fleet.</p>`],
      ['What a tryjob actually tests',`<p>A tryjob needs the patch set, its base revision, the correct DEPS dependencies, and the selected build configuration. A bot materializes that checkout before compiling and testing. Some tests are split into shards or retried. If a result is surprising, first check exactly which patch, base, builder, dependencies, and shard produced it.</p>`],
      ['From passing tests to a release',`<p>The commit queue uses configured results to decide whether a change may land on <code>refs/heads/main</code>. Post-submit builders and gardeners continue to watch for breakage after landing. Release work introduces branches: a change on main is not automatically present on a milestone branch. The supplied research describes the public configuration and scripts, but some internal Chrome performance and release automation is not visible here.</p>`]
    ],
    quiz:['A tryjob passes, but the submitted revision fails on the waterfall. What identities should you compare?', 'Patch set and base, dependency revisions, builder config, test selection and shards, plus post-submit changes. “Same code” can conceal different inputs or environment.'],
    lab:{title:'Follow one CI builder', aim:'Connect a source-controlled builder definition to the work that runs on a bot.', prediction:'Starlark config produces a Buildbucket builder, whose runs become Swarming work.', steps:['Read infra/config/main.star and identify a public builder definition.','Read the builder, build, task, and recipe entries in docs/infra/glossary.md.','Use the pinned generated config cited by the research, then draw the path from builder definition to bot task.'], observation:'The public source shows configuration and generated builder data; a live build gives execution details that are not stored here.', conclusion:'Configuration explains what should run, while build and task records show what did run.', follow:'Which details of the internal Chrome fleet are unavailable from public sources?'},
    files:[source('infra/config/main.star','Service declarations'),source('infra/config/README.md','Generated LUCI configuration workflow'),source('infra/config/subprojects/chromium/ci/chromium.linux.star','Linux Builder declaration','src',false),source('infra/config/generated/luci/cr-buildbucket.cfg','Generated builders','src',false),source('docs/infra/glossary.md','Infrastructure vocabulary'),source('docs/cq_fault_attribution.md','CQ failure reasoning'),source('docs/infra/cq.md','Commit queue overview'),source('docs/infra/builder_types.md','Builder types'),source('docs/process/release_cycle.md','Release branch process'),source('testing/buildbot/README.md','Test configuration generations'),source('tools/mb/mb.py','Builder to GN arguments'),source('infra/config/notifiers.star','Post-submit tree notifications')]
  },
  {
    id:'independent', n:'11', part:'Practice', title:'Investigate a Chromium subsystem yourself', deck:'Apply the course method to one behavior that you choose.', deps:['pixels','lifetime','factory'], skill:'Source investigation',
    lead:'By now you have seen how to find source entry points, separate process and thread ownership, read tests, and check behavior. This chapter puts those steps together. Choose one real Chromium behavior and explain it from evidence without a prepared walkthrough.',
    flow:['Choose one behavior','Find its entry point','Follow code and tests','Run a check','Write the explanation'],
    sections:[
      ['Find the relevant code',`<p>Begin with something observable: a navigation, animation, web API call, or CI failure. Read the nearest overview document to learn the names used by the subsystem. Search for an entry point, follow callers and implementations, and write down process, thread, owner, and lifetime. Then read a test that checks related behavior. A text-search match only gives you a candidate; the call path tells you whether it runs.</p><pre><code>rg -n 'SymbolName' chromium-ref/src/content chromium-ref/src/cc
rg --files chromium-ref/src | rg '(_unittest|_browsertest|web_tests)'
git -C chromium-ref/src log --oneline -- path/to/file</code></pre>`],
      ['Choose and explain one behavior',`<p>Choose a cross-site iframe navigation, a transform animation during a long task, a web API controlled by a runtime flag, or a failing CQ job. Draw the components involved. Record exact source paths and revision, then write a timeline of what happens at runtime. Compare at least two possible explanations and find a test or observation that distinguishes them.</p><p>Another engineer should be able to follow your source path, reproduce your observation, and see where your explanation is still uncertain.</p>`],
      ['When the evidence runs out',`<p>A file may be missing from the sparse checkout, a runtime flag may depend on field configuration, or a CI service may require access you do not have. State exactly what you could inspect and what you could not. Write the next source file, test, trace, or service record that would answer the open question.</p>`]
    ],
    quiz:['What should your explanation include before another engineer reviews it?', 'Include the behavior, the source path and revision, a test or observation that checks it, and any remaining uncertainty.'],
    lab:{title:'Complete your own source investigation', aim:'Write an explanation that another engineer can inspect and check.', prediction:'Write down what you expect to find before reading the implementation.', steps:['Choose one behavior from this chapter.','Draw the processes, threads, and objects you think are involved.','Find an entry point, two implementation files, and one relevant test.','Run a test, trace, or small instrumentation change if you have a build; otherwise specify how you would run it and what result you expect.','Write a short explanation that separates confirmed facts from remaining questions.'], observation:'Your first diagram will probably change after you read the code or test.', conclusion:'The completed explanation should show what happens, where it happens, and what evidence supports it.', follow:'Choose a neighboring subsystem and repeat the investigation with less guidance.'},
    files:[source('docs/debugging.md','Debugging entry point'),source('docs/trace_events.md','Trace instrumentation'),source('docs/profiling.md','Profiling methods'),source('docs/code_reviews.md','Review expectations')]
  }
];
const added = supplemental(source);
const architecture = systems(source);
export const lessons = [baseLessons[0],baseLessons[1],added.rolls,added.remote,baseLessons[2],architecture.startup,architecture.ipc,baseLessons[3],architecture.security,architecture.network,added.parsing,baseLessons[4],baseLessons[5],baseLessons[6],baseLessons[7],architecture.storage,added.features,baseLessons[8],baseLessons[9],added.release,architecture.diagnostics,architecture.journey,baseLessons[10]].map((lesson,index)=>({...lesson,n:String(index+1).padStart(2,'0')}));
